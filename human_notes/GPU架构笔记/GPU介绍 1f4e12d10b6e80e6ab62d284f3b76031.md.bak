# GPU介绍

# 1、CPU和GPU对比

## 1、特征对比

- 并行处理能力
    - CPU特点
        - 少量的强大计算单元（ALU），在很少的时钟周期内完成算术运算，时钟频率很高
        - 复杂的控制逻辑单元（Control）在程序有多个分支的情况下提供分支预测能力
        - CPU 擅长**逻辑控制和串行计算**，流水线技术通过多个部件并行工作来缩短程序执行时间
    - GPU特点
        - 数量众多的计算单元（ALU）和线程（Thread），大量的 ALU 可以实现非常大的计算吞吐量，超配的线程可以很好地平衡内存延时问题（见后续）
        - 控制单元合并多个访问
        - GPU可以同时处理多个任务，专注于**大规模高度并行的计算任务**。
- 内存架构
    - CPU利用局部性原理预取，设置**大量缓存（Cache）**来保存之后可能需要访问的数据，降低延时。
    - GPU合并相同数据的访问，**缓存很少**且为线程（Thread）服务，如果很多线程需要访问一个相同的数据，缓存会合并这些访问之后再去访问 DRMA，获取数据之后由 Cache 分发到数据对应的线程。
- 指令集
    - CPU 的指令集更加通用，适合执行各种类型的任务。CPU可以在不同的指令之间快速切换
    - GPU 的指令集主要用于图形处理和通用计算。GPU 获取**大量相同的指令（？）**并高速进行推送。
- 功耗和散热
    - CPU 的功耗相对较低，散热要求也相对较低。
    - GPU 的高度并行特性，其功耗通常较高，需要更好的散热系统来保持稳定运行。

## 2、CPU和GPU的设计目标

### 1、性能模型

- 处理器的行为可以抽象成从DRAM中加载输入，处理器进行计算，之后将结果传到cache（延迟很低），即处理器和DRAM的通信模型。
- 处理器的主要特征：处理器带宽（Bandwidth）、延时（Lantency）和吞吐（Throughput)
    - 带宽，即管道最大宽度（单位时间内最大流量）
    - 延时，处理数据的时间，或传输数据通过管道的时间（较大者）
    - 吞吐，即管道的流量（一段时间内通过的数据量）。

![image.png](GPU%E4%BB%8B%E7%BB%8D/image.png)

### 2、处理器设计逻辑

- **通用逻辑：首先提高带宽（增大流量上限），提高吞吐量通过提高线程/并行度达到，还需要追求将传送延迟隐藏在计算延迟中（提高信道利用率）。**
    - $带宽 \times 信道利用率\times 延迟 =负载$，负载（任务流）不变的情况下，**减少延迟需要增大带宽和信道利用率**
    - $带宽\times信道利用率\times时间=吞吐$，**增大吞吐（吞吐不超过负载），可以降低负载的延迟**
- CPU设计目标：在尽可能低的延迟下执行完成任务，同时保持在任务之间具体快速切换的能力。
    - 负载特点是以**序列化**的方式处理小任务，追求快速完成然后切换。
    - 小任务之间串行执行，无法通过增大吞吐/并行度降低延迟，直接缩短小任务延迟
    - 高频核心，单核设置大cache（加快内存访问），支持分支预测
- GPU 设计目标：增大吞吐量，它允许一次将尽可能多的任务推送到 GPU 内部，然后 GPU 通过大数量的 Core 并行处理任务。
    - 负载特点是大规模的**并行计算任务**
    - 对负载并行化计算，追求高吞吐，来降低整体负载的延迟
    - 设置更多核心数，增多线程数，合并内存访问。

### 3、性能指标

- 内存带宽：每秒最大传输字节量
- DRAM延迟：单次访问的延迟，比如读/写一次操作的延迟
- 每次延迟的峰值字节：单次访问的最大传输字节，即带宽/频率，或带宽*延迟/1s
- 内存利用率：带宽的使用率，即一定时间内实际传输的数据量/最大传输数据量，或单次访存传输数据量/单次访存最大传输数据量。
- 需要线程数：匹配单次访存最大传输数据量时，需要的计算线程数（计算结果能合并到单次访存）。
- 最大线程数：可并发或并行的最大线程数
- 线程比例：需要线程数/最大线程数

![image.png](GPU%E4%BB%8B%E7%BB%8D/image%201.png)

GPU（英伟达 A100）的时延比 CPU （AMD Rome 7742，Intel Xeon 8280）高出好几个倍数；

GPU 的线程数是 CPU 的二三十倍；

GPU 的可用线程数量是 CPU 的一百多倍。计算得出线程的比例，GPU 是 5.6，CPU 是 1.2~1.3，这也是 GPU 最重要的一个设计点，它拥有非常多的线程为大规模任务并行去设计。

# 2、GPU工作原理

## 1、一段代码的加速

### 1、例子

```cpp
首先通过 AX+Y 这个加法运算的示例了解 GPU 的工作原理，AX+Y 的示例代码如下：
void demo(double alpha, double *x, double *y)
{
int n = 2000;
for (int i = 0; i < n; ++i)
{
y[i] = alpha * x[i] + y[i];
}
}
```

示例代码中包含 2 FLOPS 操作，分别是乘法（Multiply）和加法（Add），对于每一次计算操作都需要在内存中读取两个数据，x[i] 和 y[i]，最后执行一个线性操作，存储到 y[i] 中，其中把加法和乘法融合在一起的操作也可以称作 FMA（Fused Multiply and Add）。

在 O(n) 的时间复杂度下，根据 n 的大小迭代计算 n 次，在 CPU 中串行地按指令顺序去执行 AX+Y 程序。以 Intel Xeon 8280 这款芯片为例，其内存带宽是 131 GB/s，内存的延时是 89 ns，这意味着 8280 芯片的峰值算力是在 89 ns 的时间内传输 11659 个字节（byte）数据。AX+Y 将在 89 ns 的时间内传输 16 字节（C/C++中 double 数据类型所占的内存空间是 8 bytes）数据，此时内存的利用率只有 0.14%（16/11659），存储总线有 99.86% 的时间处于空闲状态。

![image.png](GPU%E4%BB%8B%E7%BB%8D/image%202.png)

### 2、CPU的指令流水加速（指令并发）

由于上面的 AX+Y 程序没有充分利用并发和线性度，因此通过并发进行循环展开的代码如下，这样展开能让**编译器和CPU对其进行指令重排，让计算和传输指令互相覆盖，提高内存利用率。**

```cpp
void fun_axy(int n, double alpha, double *x, double *y)
{
for (int i = 0; i < n; i += 8)
{
y[i + 0] = alpha * x[i + 0] + y[i + 0];
y[i + 1] = alpha * x[i + 1] + y[i + 1];
y[i + 2] = alpha * x[i + 2] + y[i + 2];
y[i + 3] = alpha * x[i + 3] + y[i + 3];
y[i + 4] = alpha * x[i + 4] + y[i + 4];
y[i + 5] = alpha * x[i + 5] + y[i + 5];
y[i + 6] = alpha * x[i + 6] + y[i + 6];
y[i + 7] = alpha * x[i + 7] + y[i + 7];
}
}
```

每次执行从 0 到 7 的数据，实现一次性迭代 8 次，每次传输 16 bytes 数据，因此同样在 Intel Xeon 8280 芯片上，每 89 ns 的时间内将执行 8 次请求，内存带宽的利用率就翻了 8 倍。

现在，如果我们想要最大程度上的利用内存带宽，每 89 ns 的时间内我们最多可以执行 728（11659/16）次请求，将程序这样改进就是通过并发使整个总线处于一个忙碌的状态。

**但是在真正的应用场景中：**
编译器很少会对整个循环进行超过 100 次以上的展开；

 一个线程每一次执行的指令数量是有限的，不可能执行非常多并发的数量；

 一个线程其实很难直接去处理 700 多个计算的负荷。
由此可以看出，虽然并发的操作能够**一次性执行更多的指令流水线操作**，但是同样架构也会受到限制和约束。

**另外，这里是指令级并发，若使用多线程并发，效率或许不如单线程串行执行，因为增加额外的线程切换开销。**

### 3、CPU的多线程并行（SIMD）

将 Z=AX+Y 通过并行进行展开，示例代码如下：

```cpp
void fun_axy(int n, double alpha, double *x, double *y)
{
Parallel for (int i = 0; i < n; i++)
{
y[i] = alpha * x[i] + y[i];
}
}
```

通过**并行**的方式进行循环展开，并行就是通过并行处理器或者多个线程去执行 AX+Y 这个操作，同样使得总线处于忙碌的状态，每一次可以执行 728 个迭代。

相比较并发的方式：
每个线程独立负责相关的运算，也就是每个线程去计算一次 AX+Y；

 执行 728 次计算一共需要 728 个线程，也就是一共可以进行 728 次并行计算； 

此时程序会受到线程数量和内存请求的约束。

### 4、GPU的多线程并行（SIMT）

- GPU缓存结构
    
    英伟达 Ampere A100 内存结构中 HBM Memory 的大小是 80G，也就是 A100 的显存大小是 80G。
    
    **寄存器（Register）文件也可以视为缓存，寄存器靠近 SM（Streaming Multiprocessors）执行单元，从而可以快速地获取执行单元中的数据，同时也方便读取 L1 Cache 缓存中的数据。**
    
    **L2 Cache 更靠近 HBM Memory，这样方便 GPU 把大量的数据直接搬运到 cache 中。**
    
    GPU 设计了多级缓存：80G 的显存是一个高带宽的内存，L2 Cache 大小为 40M，所有 SM 共享同一个 L2 Cache，L1 Cache 大小为 192kB，每个 SM 拥有自己独立的 Cache，同样每个 SM 拥有自己独立的 Register，每个寄存器大小为 256 kB，因为总共有 108 个 SM 流处理器，因此寄存器总共的大小是 27MB，L1 Cache 总共的大小是 20 MB。
    
    ![image.png](GPU%E4%BB%8B%E7%BB%8D/image%203.png)
    
- GPU缓存性能
    
    GPU 和 CPU 之间的通信和数据传输主要通过 PCIe 来进行，
    
    假设使用 CPU 将 DRAM（Dynamic Random Access Memory）中的数据传入到 GPU 中进行计算，较高的时延（25 倍）会导致数据传输的速度远小于计算的速度，**因此需要 GPU 有自己的高带宽内存 HBM（High Bandwidth Memory）**。
    
    ![image.png](GPU%E4%BB%8B%E7%BB%8D/image%204.png)
    
    不同存储和传输的带宽和**计算强度（平衡存储延迟所需的计算延迟或计算操作数，多少计算操作能抵消一次存储延迟）**进行比较，假设 HBM 计算强度为 100，L2 缓存的计算强度只为 39，意味着每个数据只需要执行 39 个操作，L1 的缓存更少，计算强度只需要 8 个操作，这个时候对于硬件来说非常容易实现（封装计算操作数适中）。
    
    这就是为什么 L1 缓存、L2 缓存和寄存器对 GPU 来说如此重要。可以把数据放在 L1 缓存里面然后对数据进行 8 个操作，使得计算达到饱和的状态，使 GPU 里面 SM 的算力利用率更高。但是 PCIe 的带宽很低，整体的时延很高，这将导致整体的算力强度很高，算力利用率很低。
    
    ![image.png](GPU%E4%BB%8B%E7%BB%8D/image%205.png)
    
- GPU 线程原理
GPU 整体架构和单个 SM（Streaming Multiprocessor）的架构，SM 可以看作是一个基本的运算单元，GPU 在一个时钟周期内可以执行多个 Warp，在一个 SM 里面有 64 个 Warp，**其中每四个 Warp 可以单独进行并发的执行（？？）**，GPU 的设计者主要是增加线程和增加 Warp 来解决或者掩盖延迟的问题，而不是去减少延迟的时间。
    
    为了有更多的线程处理计算任务，GPU SMs 线程会选择超配，每个 SM 一共有 2048 个线程，整个 A100 有 20 多万个线程可以提供给程序，在实际场景中程序用不完所有线程，因此有一些线程处于计算的过程中，有一些线程负责搬运数据，还有一些线程在同步地等待下一次被计算。