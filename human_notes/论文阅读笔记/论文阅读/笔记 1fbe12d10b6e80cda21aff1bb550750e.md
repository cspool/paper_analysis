# 笔记

- 论文Heron中TensorCore的GEMM的调度模板:

```python
for i,j,r in grid(64,64,64):
	C[i][j] = C[i][j] + A[i][r]*B[r][j]
```

C的结果可以复用(模板生成规则3),C的结果由节点A、B、C产生,因此加入原语:cache-write-C(方便复用)、cache-read-C(复用C)、cache-read-A、cache-read-B(同步C的复用).

并且DLA有多层缓存(shared、fragment,适用规则2),对节点C和其生产者(A、B、C)插入分层cache读取,即share-read-A/B/C、fragment-read-A/B/C;

对三重循环分别进行tiling(适用通用规则2);

对tiling后的矩阵乘法张量化(规则1)映射到TensorCore上.

调度模板经过计算依赖生成转换后程序?

然后生成约束