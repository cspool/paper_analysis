# Paper-2026

**agent、skill**

paper主要看动机、实现、新pipeline或kernel（不对齐的机会）

**实验复现、故事思路pass**：

本次阅读全文的目的是提取复现论文实验所需的软硬件，按照下列步骤执行。

1、基于论文 Implementation / Experiment / Evaluation 部分总结：

论文如何实现 baseline；

论文如何实现所提出的创新设计；

使用了哪些硬件平台、模拟器、框架、模型、数据集或 benchmark；

为了复现，从联网搜索或从原文查找是否开源和开原链接。

2、**按实现和实验对论文的实验和实现分层次**，**输出到不同层次知识库**。

总结论文实现和实验，匹配每个层次的一般实现描述，进行分层。

不完全匹配的按照最相似/最接近来分层并反馈。

论文实现可以包含多个层次。

每个层次的一般实现描述如下。

<算法pipeline>：实现是通过稀疏、压缩、量化、蒸馏、共享或近似权重/激活等方式加速算法推理，同时保证准确率，或者新的算法模型。

属于<算法pipeline>的实现和实验按下列数据格式增加输出到“实验_算法pipeline.md”。

论文标题

属于算法pipeline的实现是什么？实验比较什么？

硬件平台是什么，配置是什么。

模型是什么。数据集和bench分别是什么。

开源情况。

<Serving调度>：实现是修改开源框架来优化多请求调度，满足SLO要求或提高吞吐量等。

属于<Serving调度>的实现和实验按下列数据格式增加输出到“实验_Serving调度.md”。

论文标题

属于Serving调度的实现是什么？实验比较什么？

硬件平台是什么，配置是什么。

开源Serving框架是什么。修改了什么。

<编译框架>：实现是修改编译框架实现，进行自动化kernel生成，保证性能等要求。

属于<编译框架>的实现和实验按下列数据格式增加输出到“实验_编译框架.md”。

论文标题

属于编译框架的实现是什么？实验比较什么？

硬件平台是什么，配置是什么。

开源编译框架是什么。修改了什么。

<kernel调度>：实现是新的kernel算子或稀疏/量化/传输等运行时计算和kernel算子在不同后端平台（GPU、异构、PIM、加速器等）的调度。

属于<kernel调度>的实现和实验按下列数据格式增加输出到“实验_kernel调度.md”。

论文标题

属于kernel调度/运行时计算的实现是什么？实验比较什么？

后端平台是什么，配置是什么。

评估性能的软件/脚本是什么。修改了什么。

<硬件架构>：实现是RTL实现IP模块或仿真器修改，来让硬件支持完成运行时计算、Tile指令、pipeline、访存、存内计算等，在workload下提升性能，并评估面积、功耗等。

属于<硬件架构>的实现和实验按下列数据格式增加输出到“实验_硬件架构.md”。

论文标题

属于硬件架构的实现是什么？实验比较什么？

模拟器名，模拟器链接（web search），或论文修改的模拟器。

模拟器模拟什么的性能，修改了什么。

<芯片设计>：实现是和物理架构关联的优化，如chiplet、Wafer-Scale Engine、DRAM、RERAM等芯片结构优化。

属于<芯片设计>的实现和实验按下列数据格式增加输出到“实验_芯片设计.md”。

论文标题

属于芯片设计的实现是什么？实验比较什么？

模拟器名，模拟器链接（web search），或论文修改的模拟器。

模拟器模拟什么的性能，修改了什么。

3、基于论文实现归属的层次，和论文原文中的Introduction、Motivation、Background 和 Method章节，说明论文通过什么具体设计解决Baseline的痛点，并按下面格式输出到“idea库.md”中。

论文标题

baseline方法是什么？给出baseline方法在模型推理算法-系统框架-编译框架-kernel调度-硬件架构全栈的执行例子。

论文方法是什么？如何对应解决 Baseline 的缺陷？对比baseline，给出论文方法在模型推理算法-系统框架-编译框架-kernel调度-硬件架构全栈的执行例子。

**学习知识pass**

根据所给论文标题，在paper_2026目录中找到原文pdf。

阅读论文背景、Preliminary、动机（baseline）和方法章节，提取关键学术术语，判断关键术语所处层次，并根据论文描述或联网搜索解释，输出到对应层次的知识库。

某层次知识库中已有相同术语，则根据新描述，补充原有术语的知识。

每个层次的一般包含的术语描述如下，据此对术语分层次并输出。

<芯片设计>：chiplet、NvLink、WSE和处于同等层次的相关学术术语。

按下面格式输出到“知识库_芯片设计.md”中：

术语名

术语解释

术语关联术语的使用例子

涉及论文标题

<硬件架构>：多GPU、XU+YU异构、GPU、PIM、加速器和处于同等层次的组成模块或相关术语。

按下面格式输出到“知识库_硬件架构.md”中：

术语名

术语解释

术语关联术语的使用例子

涉及论文标题

<kernel调度>：Sparse kernel、Attn、激活量化/稀疏的实现。Tensor Parallel（各种类似Parallel）。算子对齐硬件架构的考虑要素。kernel pipeline设计（进行overlap）、各种kernel的GPU线程实现方法/PIM call实现方法/异构系统实现方法等。

按下面格式输出到“知识库_kernel调度.md”中：

术语名

术语解释

术语关联术语的使用例子

涉及论文标题

<编译框架>：triton、sglang、pytorch等框架知识以及其内部组成的术语。整数线性规划、多面体、贪心、启发式等离线、在线算子、kernel优化方法。

按下面格式输出到“知识库_编译框架.md”中：

术语名

术语解释

术语关联术语的使用例子

涉及论文标题

<系统架构>：vllm、xxServe、serverless等框架，多请求负载的调度优化方法和概念等。请求拆分到layer、token等进行调度，动态调度请求，抢占式调度等。SM分区、动态调度SM、共享prefix、共享KVCache等。多请求调度对齐硬件架构的设计优化等。

按下面格式输出到“知识库_系统架构.md”中：

术语名

术语解释

术语关联术语的使用例子

涉及论文标题

<算法pipeline>：MoE、Transformer、VLM、LLM、DiT、LoRA、SFT、生成式推荐等模型推理的pipeline组成、流程。稀疏、压缩、量化、蒸馏、共享或近似权重/激活等加速模型推理的方式。

按下面格式输出到“知识库_算法pipeline.md”中：

术语名

术语解释

术语关联术语的使用例子

涉及论文标题

**深入扩展**（个性化）skill

对每个文章，指定特定问题。

建立Q&A库。数据结构：论文标题，Q，A。

**实验**

PIM+GPU sim：AttAcc+Ramulator2。

TensorCore Sim：**Amove**模仿TB-STC、LUT-TC，扩展Timeloop来模拟TensorCore，相比accel-sim生成GPU**行为trace**更快。

量化加速器的模拟：修改BitMod模拟器。

**focus**的VLM的三层稀疏集中硬件pipeline。focus使用gpu生成**workload trace**后输入scaleSim模拟性能。

**Q&A**

Serving的baseline和创新基于**多GPU**的任务切分（token chunk，hybrid batch）和各种queue动态调度、SM调度、pipeline动态性调度或设计。对SA加速器不太友好，相似对标是chiplet或多tile架构。

因为GPU的kernel自定义，单算子优化几乎没有空间，侧重调度。

长Context的优化基于KVCache，不同架构（多GPU 、GPU+PIM/NPU/CPU）上的调度或对齐pipeline。

硬件对多应用服务需求的适配设计，需要抽象现有Serving系统的底层硬件接口，之后设计硬件架构和关联新的接口来替换。但如何模拟新硬件接口的性能，没有实际运行会和GPU比较不公平？

硬件创新的需求：xx需要硬件提供运行时，最好直接影响性能且容易测量和比较。

HW & 算法 Co-Design

Serving本质是抽象出更多样的负载，然后直接比较硬件上的性能，而不需要考虑框架（统一相同的workload trace）。

Serving获取：workload类型，GPU的架构瓶颈。

硬件创新的工作侧重在于baseline硬件的性能测试、功耗、面积等评估。

使用paper-knowledge-cc目录中的skill，分析论文Towards High-Goodput LLM Serving with Prefill-decode Multiplexing

使用paper-experiment-idea-cc目录中的skill，分析论文Towards High-Goodput LLM Serving with Prefill-decode Multiplexing

**paper筛选**

PD-Multiplex、Bullet、Shift可以试试。

Hardwired-Neuron Language Processing Units as General-Purpose Cognitive Substrates：第一，提出 Hardwired LPU / HNLPU 这一完全硬连线 gpt-oss 120B FP4 模型的处理器范式，把权重参数物理嵌入计算结构，消除权重 fetch。

ZipServe：算法pipeline优化

现有 lossless model compression 能无损减少 LLM 权重存储，但很少真正加速 inference。原因不是压缩本身无用，而是传统熵编码和 GPU 执行模型不匹配：Huffman、ANS 这类 variable-length bitstream 需要数据相关的串行解析、查表和指针推进，会破坏 GPU warp 的 SIMT 并行。

DFVG：故事思路可抄（pipeline部分放到FPGA，单GPU不适合）

DFVG 提出一种 Draft-on-FPGA、Verify-on-GPU 的异构 speculative decoding 架构。

PAT：共享前缀，Decode Attn的运行时kernel调度。

BAT：GR Attn。

TetriServe：DiT Serving的更细调度粒度（round），因为运行时间可profile？

Laser：LLM iter、layer执行，学习。

MoDM：大小模型协作的DiT生图。

JanusQuant：2 bit推理的运行时开销。

3-bit LLM：3 bit的访问不对齐。

Sparse Transformer：MHA-mask-后续算子融合的编译框架，处理mask激活的后续算子低效。

MetaAttn：各种Attn变体的baseline。

ChituDiff：Diffusion编译。主要瓶颈来自三个层面。第一是计算与显存访问冗余：多个请求共享 prompt、LoRA、ControlNet 或中间结果时，baseline 仍可能重复执行 CLIP / U-Net / attention 等计算，或重复读取相同 K/V、权重和 conditioning。第二是调度和 batching 低效：按完全相同 shape 才 batching 会让许多请求无法合批；直接使用 ragged batch 又会为均匀 shape 请求引入额外索引和变换开销。第三是编译期组合爆炸：一个 diffusion pipeline 可有十几个甚至二十多个输入，如果为所有输入属性组合生成整图特化版本，dEngine 数量会指数增长。

MixFusion：不同分辨率切分相同patch并行。

ASM-SpMM：SpMM on Arm SME。

SpMV on TC：SpMV对齐TC。

VDHA：SpMSpV的GPU kernel

RoMeo：Text模型量化中的离群值处理，换到Video/Image呢？

AUM：CPU（+AU）的多任务调度。

GyRot：低比特量化的算法/硬件设计，多种方式需要同时支持！。

SLINFER：CPU+GPU，ServerlessLLM，调度设计，学习。

PIMPhony：PIM系统设计（学习）。

Adaptive Draft Sequence Length：SpD的PIM硬件系统设计。

bitdecoding：CC+TC作低比特量化计算和运行时。

AQPIM：激活在线量化。Product Quantization(PQ) 在线压缩 KV cache，把 KV 表示成 codebook 与 index，并在 PIM 内直接用 codebook/index 计算近似 attention，避免恢复完整 KV。

focus、VRex：Video LM

AgentBench：Agent Serving框架。（需要至少1张A100）

PASCAL：reasoning decoding？reasonging mode和一般LLM区别？

RPU - A Reasoning Processing Unit：新dataflow架构需要设计什么？

RoMe：内存控制器重构，Row粒度访问。

LeGO：单GPU同时部署SLM和图形渲染任务。

Inter-Chiplet Communication：chiplet通信。

LRM-GPU：多chiplet GPU？传统GPU？

Swift：学习SpMM的GPU kernel。

Uni-STC：四种Sp代数的统一硬件单元。

QuCo：ATT运行时配置的硬件模块。

μShare：GPU kernel运行时，提高SM不同资源使用率。

flashfuser：学习，利用DSMEM完成kernel fusion，而不需要GMEM。

VAR-Turbo：新pipeline和加速器。

AdaServe：多应用SLO要求的调度框架。

PiLLM：面向LLM Serving的token level资源管理机制，基于预测更激进分配内存同时减少OOM。

flexpipe：inflight pipeline refactoring 根据实时 CV、吞吐、延迟和队列状态选择候选 granularity，在 burst 时拆细 stage，在稳定时合并 stage。

tokenflow：交互式Streaming Serving的抢占式优化。

Fine-Grained Expert Offloading：MoE Serving，基于运行时信息预测的Expert调度，拆分Expert。

Adaptive KV Caching：Cache策略，空闲SM重算，故事思路。

MFS：model family改为嵌套model tier，新pipeline。

Module Multiplexing：多模态Serving，不同模块（VAE Encoder/Decoder、BackBone）的token走独立pipeline执行。

Test-Time Compute：TTC pipeline。

TailorLLM：大小模型的云-边协同。

TZ-LLM：权重加密infer。

AIMS：agent级联调用的云-端协同。

Computer-Use Agents：Agent友好的OS接口，学习。

FlashPS：图像编辑pipeline的Serving优化。

Cooperative Compilation and Scheduling：编译-调度协同优化，动态Tiling。