# meeting-25-12-30（实验平台）

## **meta论文**

讨论 AR：目标检测、3dgs、render、图形化算法（插帧），即增强，VR主要负载是渲染，创造；多任务场景

计算图——算子schedule——执行仿真

边缘：3dic，meta，SRAM作内存；

Meta Baseline，芯片执行面部在**头显设备**中的实时重建PiCA，算法是VAE，开销如何？

头显上的NPU或DSP执行Encoding后发送code到SoC，SoC的CPU负责接受并控制权重加载和Decoding（4 frame一个Batch并行），Decoder NN权重放在片上32MB，ML加速器执行Decoding（De-conv）后输出3D shape和纹理Map，GPU基于3D shape和纹理Map渲染到2D图像后输出到Display。

AMD XR1，头戴设备，适合复杂场景的应用

*高通AR-G1（有LPDDR），轻量级眼镜，适合轻量级应用。

![image.png](meeting-25-12-30%EF%BC%88%E5%AE%9E%E9%AA%8C%E5%B9%B3%E5%8F%B0%EF%BC%89/image.png)

## 方向

diffusion新的并行

多依赖的layout变换优化：单model也能产生多任务

diffusion加速器

每层输入的稀疏，跳过计算，输出结果的编码，跳过下一层，资源分配；

**motivation**：diffusion，图像应用，图像高维，深度信息、边缘信息空白；因此方法创新最重要。

TPU设计的思路，GPU都有内存；

实验依靠模拟器

transformer中的layernorm能被tanh替代：Stronger Normalization-Free Transformer。

也能被误差函数erf替代，但erf是个高斯函数的积分。用于DiT？

qwen-image-layered 速度很慢，图像分图层 

turbo-diffusion生成视频

## future

不同场景：中心加速器GPU、TPU内存稍大，端侧更小内存。

dataflow拆分模型，一层的weights无法加载而且慢，优化减少复用，论文用小模型。

云边端应用：小模型驱动大模型，传递特征，大模型处理多个任务；3dgs优化，多个人共享事物，同步多人位置关系，联机；

模型的结构抽象，拆分，促进云边端；

模型的本地部署；

DiT！！！：实验平台，仿真，架构实现，实践修改。！！！

ref：pisa

架构优化技术很常见，重点在于用于实现不同的应用，达到加速或者降内存的效果（别人用什么平台，我就用什么）。

[https://www.usenix.org/conference/osdi25/presentation/shen-weihang](https://www.usenix.org/conference/osdi25/presentation/shen-weihang)

测试原型机，非GPU，TPU+X（DSP or CPU）

网络轻量化；

算子CFG模拟，算子loading到FPGA；

R-V instr仿真器作执行，流片，。

通用算子，schedule融入Arch，**软件不支持新的schedule方法**。

input动态性。

arch：token吞吐率。

大模型拆分：llm修饰提示词，diffusion放本地。

AR项目三个应用：两个NN，3D重建，双目multi-sensor。

插帧生成，左眼生成>右眼基于左眼。