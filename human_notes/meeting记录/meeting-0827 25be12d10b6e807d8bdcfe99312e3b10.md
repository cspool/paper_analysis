# meeting-0827

参考：弹性推理系统

# 动态资源分配的系统级支持

## 弹性机制、动态资源分配需求：

新一代应用：多任务并行，计算内存sharing，

可重构加速器特点：以时延换来的高吞吐？重构开销换取效能提升？

（memory copy，control latency，compile， mapping）

多用户？： 用户请求变化

导致（大模型）应用的突发性负载变化显著： 

1、内存波动 2、资源抢占 3、冷热应用QoS问题，热应用具有低时延需求

## 弹性机制

1 并行方法切换（软件架构） （pipeline 优化），

2.1 异构架构 

2.2 动态资源分配 （系统） 

3 context switch （硬件架构）

reset-based preemption、

switch-based preemption、 

memory中专 ？？

4 电路设计 （memory 共享）

## 架构设计

1. 计算资源分配：解放交互、灵活性（分块分组填充）、量化 2. 多并发支持，运行时

清华的陆游游老师 GPU context switch

前面几章可以参考一下需要什么数据

[**GPreempt: GPU Preemptive Scheduling Made General and Efficient（GPU抢占优化）**](GPreempt%20GPU%20Preemptive%20Scheduling%20Made%20General%20an%2025ce12d10b6e803e9cb3fa198edf81dc.md)

reorder隐藏在psum reduction；

[FEATHER: A Reconfigurable Accelerator with Data Reordering Support for Low-Cost On-Chip Dataflow Switching](FEATHER%20A%20Reconfigurable%20Accelerator%20with%20Data%20Reo%20249e12d10b6e80a291f6c84164f41a66.md)

reorder隐藏在psum compute；

多任务共享？

CGRA 基于DFG计算编译成CGRA，生成计算节点和DFG的映射关系，

输入DFG，动态找到最优mapping，动态需要搜索速度快，动态数据传输，动态任务，动态的资源分配；

切换任务开销，外存IO到片上存储（数据迁移），并发任务，资源分配改变；

多任务并发的fusion效果、实现；

fusion后任务1需要yield，但任务2可以继续；

动态性：多任务全局最优（建模不准，动态检测），突发需求，需要动态指标；

动态编译——>静态编译，动态修改；

——>ML Acc、异构

冷热任务