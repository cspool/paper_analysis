# 记录-2025/05/13

# 1、混精CNNs加速器概述

## 1、整体工作流

![image.png](%E8%AE%B0%E5%BD%95-2025%2005%2013/image.png)

### 1、主要流程

1. 上电后，DSP完成预处理和数据重排，初始化DSP的DDR数据，即准备好FPGA-DDR的数据镜像。
2. DSP通过SRIO接口，用数据镜像初始化FPGA的DDR数据。
3. DSP通过EMIF接口初始化FPGA的指令RAM、bias参数RAM、E参数RAM、scale参数RAM。
4. DSP给出启动信号，启动FPGA端进行模型推理。或DSP给出debug信号，要求从指定指令开始执行。
5. FPGA端进行模型推理，或指定指令的执行；完成后给出结束信号，或当前指令完成信号。
6. DSP将FPGA最后的输出结果从FPGA-DDR读取到自身DDR进行后续计算，即检测头的sigmoid与后处理的NMS计算。

### 2、角色总结

0-DSP_core0（预处理和模型加载）：DSP作输入、权重的预处理，片上初始化(DSP-DDR上权重预载与片上buffer上的量化参数预载)。

1-DSP_core1（输入加载）：DSP负责输入特征图的加载(DSP-DDR)。

2-FPGA（片上加速）：DSP启动FPGA加速主要算子(FPGA-DDR)。

3-DSP_core2（后处理的浮点计算）：FPGA输出交给DSP作后处理(检测头卷积之后的处理).

负责1、2、3的三个角色组成流水线完成图像流的推理加速。

## 2、FPGA端的设计

### 1、指令

量化计算方法总结: 

[量化方法的总结](../knowledge%20chips/%E9%87%8F%E5%8C%96%E6%96%B9%E6%B3%95%E7%9A%84%E6%80%BB%E7%BB%93%201cfe12d10b6e81ea95f5dc1a7a071fad.md)

- CBR(conv-BN-ReLU)
    - 输入 $q_a$是uint8,输入$q_a$是int8或int1,输出 $q_o$为uint8.
    
    $q_{o} = \left\{ \begin{matrix}
    \frac{clamp((\sum_{}^{}{q_{w} \times q_{a} \times q_{E}} + q_{bias}),0,8 \times 256 \times 255)}{8 \times 256},q_{w}:1bit \\
    \frac{clamp((\sum_{}^{}{q_{w} \times q_{a} \times 1} + q_{bias}),0,8 \times 128 \times 255)}{8 \times 128},q_{w}:8bit
    \end{matrix} \right.$
    
- 残差
    - 输入 $q_{cv}$是int32,输入$q_{bypass}$是int32,输出 $q_{sc}$为int32.
    
    $q_{sc}=clamp( \frac { q_{cv}}{s_{bypass}\times 8}+  \frac { q_{bypass}}{s_{cv}\times 8},0, \frac {1}{s_{cv} \times s_{bypass}}) ,s_{sc}= {s_{cv} \times s_{bypass}}$
    
- 池化
- 上采样

### 2、架构

类似CISC的设计理念,指令的粒度较粗,比如卷积层(CBR)为一条指令,其**数据存储和计算控制完全由硬件负责实现**,FPGA推理时,没有其他芯片辅助计算或控制.

### 3、关键技术

- 基于DSP48E打包的混精度乘累加器——提高每个DSP的并行度
    - MAC结构图
        
        ![image.png](%E8%AE%B0%E5%BD%95-2025%2005%2013/image%201.png)
        
    - 有符号修正公式
        
        ![image.png](%E8%AE%B0%E5%BD%95-2025%2005%2013/image%202.png)
        
- 基于片上img2col的矩阵乘法(MM)实现卷积
    - img2col能够完全复用重叠数据
    - 固定的数据流(固定的并行方式)
        
        ![image.png](%E8%AE%B0%E5%BD%95-2025%2005%2013/image%203.png)
        
    - 理想情况支持更多的并行方式,能得到更高的资源使用率,但没有设计
        
        ![image.png](%E8%AE%B0%E5%BD%95-2025%2005%2013/image%204.png)
        
    - 脉动阵列实现MM(数据、指令、结果的脉动,时序更优)
        
        ![image.png](%E8%AE%B0%E5%BD%95-2025%2005%2013/image%205.png)
        
- 计算与存储的流水覆盖
    - 完全复用输入,每块权重用完就丢
        
        ![image.png](%E8%AE%B0%E5%BD%95-2025%2005%2013/image%206.png)
        
    - 其余模式有过设计但没有实现
    - 不同模式下的覆盖率不同,延迟也不同,可建模得到.

## 3、其余想法

### 1、支持attention

yolo10、11

### 2、灵活的并行方式设计

### 3、计算、传输的设计与延迟的建模

每层最优延迟的计算传输模式不同,需要运行时配置,但如何控制硬件设计复杂度,最直白的方式是每种模式一个控制器

### 4、硬件的译码、控制很复杂,且性能不佳,实现方式或可更优.

指令可能需要更细致,比如RICS的设计思路

### 5、编译时的算子融合

计算传输的覆盖率由参数决定,部分层就是IO-bound,可能考虑让其与前/后邻接层融合;

量化时,中间结果可能需要保留高精度(int32),比如检测头的conv输出,比如残差的两个输入,对这些算子,考虑其与前/后邻接算子融合.

### 6、问题:缺少一些方向

目前在学习各种芯片的设计.

# 2、meeting速记

## 0、一些框架&一些方法

### 1、AI的软硬件协同框架

![image.png](%E8%AE%B0%E5%BD%95-2025%2005%2013/image%207.png)

### 2、研究思路：AI推理的加速

- 横向延申：支持不同模型的推理加速、模型到不同平台的任务映射。
    - 不同模型到某个平台的编译过程
        - 不同的任务负载特点，如何自动化编译得到某平台的指令序列
    - 模型到平台A的后端编译结果，如何转换成到平台B的后端编译结果（tuning）
        - 为什么不重新编译呢？因为可以减少重新开发后端编译的研究？
- 纵向延伸：模型构建、模型轻量化、模型编译、模型部署、加速器体系架构。

## 1、方向调研

核心：不局限FPGA，而是侧重XPU之类的加速器设计。体现差异性,假设一个场景,设计有优势（新架构）。

- Soter：Analytical Tensor-Architecture Modeling and Antomatic Tensor Program Tuning for Spatial Accelerator
- **端到端加速**：**模型推理过程完整在片上,而非单模块加速，减少很多外部的交互**；与其相对的是逐kernel（算子）计算，即由master调用， 调用一次只算一层或一块.
    - 端到端意味着**理论上中间结果可以不存储到memory**，但实际上片上存储受限，因此需要作流水优化和排布。
    - **端到端的流水优化和子任务排布**
- 编译时**算子融合**:两个算子融成一个算子.
    - 寻找现有融合算法的局限性
    - 中间结果是否隐藏，即放在片上，不需要存储（**量化时，高精度算子和其前/后邻接算子可能更需要融合**）
    - 多个操作数，加载可能产生额外的延迟,load miss
- **spMM**：算法层很成熟,体系结构探索不多
    - 实践来看，输入矩阵的dense到sparse编码的延迟很高，导致性能和速度不理想
- **新的模型提出新的需求/背景，就可以提出新的体系结构，比如CNN+ATT**
- **follow的工作**
    - US：耿tong，tong geng，boston
    - CN：gao mingyu，TSU
    - US：ding yufei，CA
- 体系结构四大会:ISCA,HPCA,MICRO,ASPLOS

## 2、想法

- CGRA的动态编译：动态即运行时，编译即指定CGRA的结构；即**运行时改变电路的连接情况**，让CGRA能支持多种并行方式，从而提高资源使用率。
    - 换成FPGA，编译的速度很慢，不可能在运行时改变电路连接情况，可行的方式是可重配置的体系结构，即运行时改变参数，从而改变并行方式。
    - **CGRA的动态编译的技术难点在于加快编译速度，使其不拖累推理性能；FPGA的运行时配置的技术难点在于通过静态的结构设计和可配置的参数，达成支持多种并行方式的效果。**
    - sumbanova system在做大模型向CGRA的高效映射的尝试。
- 大模型在嵌入式设备的推理
    - 推理模式可能是CPU调用加速核进行加速，这需要共享内存。一个思路是使用RDMA，加速核直接访问内存。
    - **但内存大小受限？导致效果不好？**
    - 依托MOE机制，将模型动态载入？利用CPU上实现的快速查找算法来完成routing。
- 混精CNNs推理：能否去除DDR的加载？直接从CPU/DSP的DDR中去取数据？不load到FPGA的DDR？
    - 目前FPGA独立访问DDR，不需要DSP参与，计算推理时间时不包含DDR的数据初始化，但**不同图片的推理时，仍然需要将DSP的DDR中的输入加载的FPGA的DDR。**
- **动态映射的需求：目前混精度量化的方式是，通过信息熵指标，给不同的计算块不同的精度，每个块进行卷积计算，之后将这些块的输出通过transformer来提取全局特征。**
    - 指令的粒度要更细：块的存储指令、块的计算指令，否则硬件的设计复杂度过高，且计算-存储的流水优化的自由度下降。
- **不同平台的tuning编译**：不同平台支持的底层算子的精度、并行方式不同，针对每一个平台作上层操作的高性能实现（可视为操作排布和并行方式）是一种方式，更理想的想法是**基于已有平台的高性能实现参数（经过验证且可靠），通过调整（tuning）得到其他平台的高性能实现。**
    - 本质上，算子的精度、并行方式是平台的通用属性，某平台上的算子的高性能实现也是基于这些通用属性建模得到的，因此基于既有实现，通过其余平台的属性进行调整，得到其余平台的高性能实现是理论可行的。
    - 这个调整过程，可能引入ML或者DL算法。
    - 或能**帮助异构平台快速构建生态。**
- GPU、XPU设计后都需要在FPGA上验证，之后才能流片，因此是否能在FPGA上实现XPU，这样就能找到统一的度量来进行性能评估，从而比较不同XPU的性能，进而指导异构或芯片设计。
    - 但由于XPU架构保密，很难完全用FPGA来等效。