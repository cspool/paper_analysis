# 生成式模型基础（MLE、VI、OT）

## 未知量的估计（先验、似然、后验）

### **未知量估计：频率派 vs. 贝叶斯派**

某硬币朝上概率是未知数**θ**，通过统计朝上的次数的实验（**采样**）估计未知数θ的值。

某图像的归属类别是未知量**θ**，通过提取图像特征的实验（**采样**）估计归属的类别θ。

**频率派**认为概率是无穷次试验中样本出现的频率，即θ是样本本身的属性（无分布），样本发生概率是似然概率。

**贝叶斯派**认为概率是观测者对事件发生的主观可信度，即所有事物（θ）都是随机变量（有分布）。样本发生概率是隐变量先验Prior和样本似然概率Likelihood的联合概率。

先验分布定义**未知量θ的初始认知（取值范围充分大但不确定性高）**，θ的**后验分布**在无限含有θ信息量的样本作为条件时，收敛到狄拉克分布δ（μ=频率派概率，σ≈0）。

![image.png](%E7%94%9F%E6%88%90%E5%BC%8F%E6%A8%A1%E5%9E%8B%E5%9F%BA%E7%A1%80%EF%BC%88MLE%E3%80%81VI%E3%80%81OT%EF%BC%89/image.png)

![image.png](%E7%94%9F%E6%88%90%E5%BC%8F%E6%A8%A1%E5%9E%8B%E5%9F%BA%E7%A1%80%EF%BC%88MLE%E3%80%81VI%E3%80%81OT%EF%BC%89/image%201.png)

### **极大似然估计MLE**

实验得到的**样本**是随机事件**X**的某个取值，通过分析随机事件X发生的概率来估计θ。

随机变量X遵从带有参数θ的分布，**样本x发生的概率P(X=x; θ)**是θ的函数，因此定义**似然（概率）函数**，记为**L(θ|X=x)=P(X=x; θ)**，表达样本x在不同θ取值下的发生概率，任一θ取值下样本x发生的概率是**似然概率**。

样本x是实验的结果，认为样本x发生的概率很高，求取**L(θ|X=x)的极值点**作为θ的估计值，$\theta_{\text{MLE}} = \underset{\theta}{\arg\max} \ L(\theta|\text{data}) =\underset{\theta}{\arg\max} \ P(\text{data};\theta)$ 。

### **最大后验估计MAP**

随机事件X和随机变量θ遵循未知联合分布，**样本x发生的联合概率P(X=x,θ=θ0)**=先验概率P(θ**=**θ0) * 在θ=θ0条件下的样本x发生的**条件概率P(X=x|θ=θ0)** 。

样本x发生的**联合概率最大处的θ取值**是估计值，$\theta_{\text{MAP}} = \underset{\theta}{\arg\max} \ P(\theta|\text{data}) \propto \underset{\theta}{\arg\max} \ P(\text{data}|\theta)P(\theta)$。高维的联合分布不便于直接找到单变量的最优估计，因此定义后验概率来计算样本x发生时不同θ取值的概率。**后验概率P(θ=θ0|X=x)**是样本x发生由随机变量θ=θ0导致的概率，P(θ=θ0|X=x) = 联合概率P(X=x,θ=θ0) / **X边沿概率P(X=x)**。X边沿概率P(X=x)的计算是通过P(θ|X)在θ上的积分或者求和。

样本x发生由随机变量θ导致这一随机事件**遵循X=x为条件下的θ的条件概率分布，**样本x的后验概率P(θ|X=x) =P(X=x,θ) / P(X=x)。**选取P(θ|X=x)最大处的θ**作为估计值。

### MLE vs. MAP：似然“函数”和后验“概率”

**似然概率（likelihood）**是**参数（如Weights）**某个取值下对事件发生的**概率预测**，**后验概率（probability）**是事件发生由随机变量的某个取值导致的**概率**。因此模型学到的都是似然概率。

*似然Likelihood vs. 概率Probability*

![image.png](%E7%94%9F%E6%88%90%E5%BC%8F%E6%A8%A1%E5%9E%8B%E5%9F%BA%E7%A1%80%EF%BC%88MLE%E3%80%81VI%E3%80%81OT%EF%BC%89/image%202.png)

![image.png](%E7%94%9F%E6%88%90%E5%BC%8F%E6%A8%A1%E5%9E%8B%E5%9F%BA%E7%A1%80%EF%BC%88MLE%E3%80%81VI%E3%80%81OT%EF%BC%89/image%203.png)

MLE和MAP的统一逻辑是**最大化样本x发生的概率**，但频率派和贝叶斯派对未知量θ和实验样本有不同理解（参数 vs. 随机变量），分别使用似然函数和样本后验概率来估计θ。

似然（概率）函数**L(θ|X=x)**，自变量是θ，在任一**θ取值**下的函数值是似然概率，即对应θ取值下样本**x**发生的概率。因为参数θ**不遵循分布**，不能保证所有θ取值的似然概率累加等于1，因此似然函数**没有归一性**。

样本x的后验概率**P(θ|X=x)**，自变量是θ，在任一**θ取值**下的函数值是后验概率。因为P(θ|X=x)是X=x条件下的**θ的条件概率分布**的函数值，因此P(θ|X=x)**满足归一性**。

样本x的似然函数表达**随机变量X=x的概率**和参数θ的关系，样本x的后验分布是随机变量X=x的条件下**随机变量θ的条件概率分布**。

频率派估计是贝叶斯派估计的极限情况。

*其他概率知识*

***概率分布函数F(X≤x)**等价于概率分布P(X)，**F定义成P的累加，P则是F的差分**，如此让F的差分和导数拥有有效含义，也能定义出连续性随机变量的概率密度函数f。*

*总分总结构*

**总起**（直观易懂抽象地介绍论点、关键概念）——**分解**（逐个拆解论点逻辑、概念解释）——**总结**（深刻回顾论点，围绕论点稍微引申）

## 判别式和生成式学习（假设、学习分布）

### 对分布的假设（先验假设、似然假设）

贝叶斯理论中**先验概率**描述对未知量的认知（无样本），**条件概率**描述未知量对样本发生的作用（证据），**后验概率**描述样本修正后对未知量的认知（有样本）。

*Q1：贝叶斯理论的实际应用中，当分布未知时会假设分布，**为什么假设**的分布有作用？*

A1：目标是**估计变量Z取值**时，变量Z的分布是信息量低的**初始认知**，条件分布（证据）能够**修正不准确**的初始认知，得到**相对正确**的Z估计，但是**先验不能偏差过大**（比如取值范围过窄），故一般假设高斯。

A2：目标是**建立隐变量Z分布和样本X分布的映射p(X | Z)和p(Z | X)**，**来学习样本X的分布**，定义隐变量分布p(Z)，便于生成p(X)，即先验分布p(Z)是生成p(X)的工具。

变分推断优化：定义p(Z)，**学习和建立p(Z)和p(X)之间的映射**；ELBO中reconstruction项要求pθ(X | Z)从Z中提取X的信息，也要求qφ(Z)蕴含更多X的信息；regularization项要求样本X映射到p(Z)范围中，减少qφ(Z)由其他样本Y采样生成的可能。

![image.png](%E7%94%9F%E6%88%90%E5%BC%8F%E6%A8%A1%E5%9E%8B%E5%9F%BA%E7%A1%80%EF%BC%88MLE%E3%80%81VI%E3%80%81OT%EF%BC%89/image%204.png)

![image.png](%E7%94%9F%E6%88%90%E5%BC%8F%E6%A8%A1%E5%9E%8B%E5%9F%BA%E7%A1%80%EF%BC%88MLE%E3%80%81VI%E3%80%81OT%EF%BC%89/image%205.png)

*Q2：假设先验分布是标准高斯分布，条件分布服从未知参数的高斯分布，**为什么是高斯**？*

当分布需要假设时，高斯分布是最合适的，因为大部分随机变量序列**累加的极限分布**是高斯分布。

高斯变量经过许多**变换构造**的随机变量仍是高斯分布，假设高斯便于计算，是模型的简化。

高斯分布既包含hard edge又包含sparsity特征，能“零知识”的**平滑适应**大部分数据分布。

![image.png](%E7%94%9F%E6%88%90%E5%BC%8F%E6%A8%A1%E5%9E%8B%E5%9F%BA%E7%A1%80%EF%BC%88MLE%E3%80%81VI%E3%80%81OT%EF%BC%89/image%206.png)

![image.png](%E7%94%9F%E6%88%90%E5%BC%8F%E6%A8%A1%E5%9E%8B%E5%9F%BA%E7%A1%80%EF%BC%88MLE%E3%80%81VI%E3%80%81OT%EF%BC%89/image%207.png)

*Q3：**Online Bayes**意味着后验分布是checkpoint，但**limit不是先验分布。***

Online Bayes利用**基于当前样本的后验概率正比于基于过去样本的后验概率**的理论事实，随着样本增加而降低后验分布不确定性。

> 先验p(Z)是对**未知量**的**初始认知**（取值范围广但不确定度高），**后验不收敛于先验**，而是收敛于变量的频率派估计值的δ分布。Online Bayes是后验分布的在线学习算法，根据样本逐渐优化对Z的认知（后验分布）。

> 变分推断模型**建立隐变量Z和样本X的采样映射**，从定义分布p(Z)中采样生成样本X的分布，需要样本的出现概率**p(X)最大**，要求近似后验分布**qφ(Z)接近p(Z)，只用于生成p(X)**。

![image.png](%E7%94%9F%E6%88%90%E5%BC%8F%E6%A8%A1%E5%9E%8B%E5%9F%BA%E7%A1%80%EF%BC%88MLE%E3%80%81VI%E3%80%81OT%EF%BC%89/image%208.png)

![image.png](%E7%94%9F%E6%88%90%E5%BC%8F%E6%A8%A1%E5%9E%8B%E5%9F%BA%E7%A1%80%EF%BC%88MLE%E3%80%81VI%E3%80%81OT%EF%BC%89/image.png)

![image.png](%E7%94%9F%E6%88%90%E5%BC%8F%E6%A8%A1%E5%9E%8B%E5%9F%BA%E7%A1%80%EF%BC%88MLE%E3%80%81VI%E3%80%81OT%EF%BC%89/image%209.png)

### 判别式和生成式学习分布

*MLE中随机变量X遵循的**未知**分布，MAP中随机变量X和θ遵循的**未知**分布，**如何获取**？*

模型学习分布完成目标任务：

> 目标检测模型学习以类别为条件的图像条件分布（MLE）。

> 自回归模型学习以上下文为条件的下一个Token的条件分布（MLE）。

> Encoder学习以样本为条件的隐变量的后验分布，来编码图像或其他样本。（VI）

> 生成式模型将**样本看作对隐变量若干次采样的结果**，如**将图像看作对pixel变量作Dx×Dy次采样的结果**，生成式模型学习样本分布，进行样本的生成（采样）。

**判别式**Model包括频域样本的CNN、时域样本自回归的LSTM和Transformer等，使用标签Y以样本X为条件的条件概率分布进行推理。**生成式**Model包括传统自编码器、变分自编码器VAE、Diffusion等，学习样本X自身的分布或样本X以标签Y为条件的条件分布进行推理。

MLE、VI、OT是模型学习似然分布的**理论模型**，包含设计Loss函数来指导Weights如何取值更优，比如logP，KL(Truth || Model)，KL(Model || Truth)，ELBO等。

根据已有数据判断是否容易**收敛**（分布稀疏用OT、VI而不是MLE）来选择不同理论模型。

ref：[https://zhuanlan.zhihu.com/p/597381044](https://zhuanlan.zhihu.com/p/597381044)

![image.png](%E7%94%9F%E6%88%90%E5%BC%8F%E6%A8%A1%E5%9E%8B%E5%9F%BA%E7%A1%80%EF%BC%88MLE%E3%80%81VI%E3%80%81OT%EF%BC%89/image%2010.png)

![image.png](%E7%94%9F%E6%88%90%E5%BC%8F%E6%A8%A1%E5%9E%8B%E5%9F%BA%E7%A1%80%EF%BC%88MLE%E3%80%81VI%E3%80%81OT%EF%BC%89/image%2011.png)

*其他分布推断*

专家知识给出：如投硬币实验中X遵循以θ为参数的二项分布，在MLE中是随机变量X遵循的带参数分布，在MAP中是随机变量X以θ为条件的概率分布。

人为假设分布：如投硬币实验中θ的先验分布可能是人为假设的高斯分布、伯努利分布等。

经验贝叶斯：假设先验分布P(θ)属于已知分布簇但包含参数λ，用历史样本优化λ。

马尔可夫链蒙特卡洛MCMC：随机游走方式采样样本空间，估计样本x的概率分布。

### 变分推断VI（建立隐变量Z和样本X的映射）

ref：[https://zhuanlan.zhihu.com/p/1900299484749072056](https://zhuanlan.zhihu.com/p/1900299484749072056)

[https://zhuanlan.zhihu.com/p/348498294](https://zhuanlan.zhihu.com/p/348498294)

**隐变量模型**：设定分布$p(z)$，$p(z)$上的“点云”通过$p(x | z)$采样生成$p(x)$上的“点云”，$p(x)$上的“点云”通过$p(z | x)$采样生成$p(z)$上的“点云”，即$p(x | z)$是$p(z)$到$p(x)$的采样映射，$p(z | x)$是$p(x)$到$p(z)$的采样映射。

![image.png](VAE%E3%80%81VQVAE%E3%80%81VQGAN%EF%BC%9A14%E3%80%8119-22/image%201.png)

![image.png](VAE%E3%80%81VQVAE%E3%80%81VQGAN%EF%BC%9A14%E3%80%8119-22/image%202.png)

**参数化$p(x | z)$，最大化$p(x)$的MLE优化方法**

![image.png](VAE%E3%80%81VQVAE%E3%80%81VQGAN%EF%BC%9A14%E3%80%8119-22/image%203.png)

![image.png](VAE%E3%80%81VQVAE%E3%80%81VQGAN%EF%BC%9A14%E3%80%8119-22/image%204.png)

$p(Z)$采样Z生成的样本Y不一定在分布$p(X)$中，因为$p(Z)$和$p(X)$在各自高维空间中是**稀疏的**，**难以找到配对的数据**训练**$p_θ(X | Z)$**。

后验分布**$p_θ(Z | X)$能将样本分布$p(X)$采样映射到$p(Z)$**，即将$p(X)$中**”相似的“样本**映射到$p(Z)$中，使用**配对的$p_θ(Z | X)$采样Z’和样本X**学习条件分布**$p_θ(X | Z)$**。

**后验分布$p_θ(Z | X)$的计算需要$p_θ(X)$，配对数据同样稀疏**。参数化后验概率的**近似$q_φ(Z | X)$，$q_φ(Z | X)$是优化的”启动器“。**

变分推断模型提供了同时学习**$p_θ(X | Z)$和$q_φ(Z | X)$的方法**

![image.png](VAE%E3%80%81VQVAE%E3%80%81VQGAN%EF%BC%9A14%E3%80%8119-22/image%205.png)

![image.png](VAE%E3%80%81VQVAE%E3%80%81VQGAN%EF%BC%9A14%E3%80%8119-22/image%206.png)

**ELBO尽量大**，则$q(z)$对后验分布$p(z | x)$的近似程度更好，将ELBO分解成重建项$Eq(logp(x | z))$和正则项$KL(q(z) || p(z))$。

> 重建项$Eq(logp_θ(x | z))$越大越好，让分布$q(z)$中采样$z$生成的$x'$接近真实值$x$，$q(z)$蕴含$x$更多信息$q(z)$。

> 正则化项$KL(q(z) || p(z))$越小越好，让$q(z)$近似$p(z)$，让**”相似“的样本$x$映射到$p(z)$范围中**，减少$q(z)$由其他分布$p(y)$的样本$y$采样生成的可能。$q(z)$和$p(z)$一般不相等，因为总有其他分布$p(y)$的样本$y$采样生成到$p(z) =\int_{D^2} p(z | x) p(x)dx$ 。