# 模拟器-Gemmini

[https://github.com/ucb-bar/gemmini/tree/master](https://github.com/ucb-bar/gemmini/tree/master)

WS、OS的GEMM，WS的Conv。

![Screenshot from 2026-04-13 19-11-07.png](%E6%A8%A1%E6%8B%9F%E5%99%A8-Gemmini/Screenshot_from_2026-04-13_19-11-07.png)

![Screenshot from 2026-04-13 19-12-52.png](%E6%A8%A1%E6%8B%9F%E5%99%A8-Gemmini/Screenshot_from_2026-04-13_19-12-52.png)

![Screenshot from 2026-04-13 19-13-29.png](%E6%A8%A1%E6%8B%9F%E5%99%A8-Gemmini/Screenshot_from_2026-04-13_19-13-29.png)

![Screenshot from 2026-04-13 19-12-40.png](%E6%A8%A1%E6%8B%9F%E5%99%A8-Gemmini/Screenshot_from_2026-04-13_19-12-40.png)

### 通用架构

SPAD是scratchpad（SRAM），其他实现是Shared Memory或Buffer。

---

```bash
Gemmini 脉动阵列 DL 加速器深度解析

---

1. 模型推理模拟原理

驱动模式：Cycle-Accurate + RoCC 指令驱动

Gemmini 不是 Trace 驱动，也不是纯事件驱动，而是 Cycle-Accurate RTL 仿真，以 RISC-V RoCC 自定义指令 为驱动接口。

RISC-V Rocket Core
│  custom0 指令流 (XCUSTOM_ACC)
▼
io.cmd (RoCCCommand)

CPU 通过 ROCC_INSTRUCTION_RS1_RS2(XCUSTOM_ACC, rs1, rs2, funct) 宏编码并发射指令，Gemmini
硬件每周期推进一步，所有寄存器/SRAM/流水线状态都是精确到时钟周期的。

组件交互架构图

DRAM
│  TileLink (L2 Cache 总线)
▼
DMA Engine (in Scratchpad.scala)
│        ▲
│  mvin  │  mvout
▼        │
┌─────────────────────────────────┐
│        Scratchpad SRAM          │
│  4 banks × 64KB  (sp_width=128b) │  ← Controller.scala:35
│  Accumulator 2 banks × 32KB     │
└──────────┬──────────────────────┘
│  ScratchpadReadIO (per bank)
▼
┌──────────────────────────────────────────────────────┐
│              ExecuteController                        │  ExecuteController.scala
│  ┌──────────┐    ┌───────────────────┐               │
│  │ Im2Col   │◄──►│  TransposePreload  │               │
│  │  Unit    │    │  Unroller          │               │
│  └──────────┘    └────────┬──────────┘               │
│                           │  preload/compute 指令      │
│                           ▼                           │
│              ┌────────────────────────┐               │
│              │      Mesh (脉动阵列)    │  Tile.scala   │
│              │  meshRows × meshColumns │               │
│              │  = 16 × 16 个 PE       │  PE.scala     │
│              └────────────────────────┘               │
└──────────────────────────────────────────────────────┘
▲
│ issue.ld / issue.ex / [issue.st](http://issue.st/)
┌──────────┴──────────────────────┐
│      ReservationStation (ROB)   │  ReservationStation.scala
│  (ld 8项, ex 16项, st 4项)       │
└──────────┬──────────────────────┘
│ alloc
▼
LoopMatmul Unroller         LoopConv.scala / LoopMatmul.scala
▲
LoopConv Unroller
▲
raw_cmd_q (Queue depth=2)
▲
io.cmd (CPU RoCC 指令)       Controller.scala:237~244

三条流水线并行（Controller.scala:301~303）:

- LoadController → Scratchpad DMA 读 (mvin)
- StoreController → Scratchpad DMA 写 (mvout)
- ExecuteController → PE Array 计算

---

1. Example 运行细节：ResNet50 Conv 层

两种模式

模式 A：纯软件 im2col + tiled_matmul（resnet50.c:75~88）
im2col(...)                          // CPU 做展开，写入 conv_1_in[]
tiled_matmul_nn_auto(I, J, K, ...)  // 发射 gemmini_loop_ws 指令组

模式 B：硬件 tiled_conv（conv=true 时，默认路径）
tiled_conv_auto(...)  // 发射 gemmini_loop_conv_ws 指令组

关键函数调用链（WS matmul 路径）

resnet50.c:main()
└─ tiled_matmul_nn_auto()                  gemmini_nn.h:127
└─ tiled_matmul_auto()                gemmini.h
└─ sp_tiled_matmul_ws()          gemmini.h:~450
├─ gemmini_config_ex(WS,...)            // CONFIG 指令 funct=k_CONFIG
├─ gemmini_extended_config_ld(stride,..)// CONFIG_LD
└─ gemmini_loop_ws(I,J,K,...)           // gemmini.h:366
├─ k_LOOP_WS_CONFIG_BOUNDS  (funct=9)
├─ k_LOOP_WS_CONFIG_ADDRS_AB (funct=10)
├─ k_LOOP_WS_CONFIG_ADDRS_DC (funct=11)
├─ k_LOOP_WS_CONFIG_STRIDES_AB(funct=12)
├─ k_LOOP_WS_CONFIG_STRIDES_DC(funct=13)
└─ k_LOOP_WS             (funct=8)  ← 触发硬件展开

硬件侧时序（LoopMatmul FSM 展开后 → RS → 三控制器）

Clock  │ CPU 发 k_LOOP_WS │ LoopMatmul 硬件展开为低级指令序列:
T0   │ k_LOOP_WS        │
T1   │                  │ → CONFIG_EX (WS, shift, acc_scale)
T2   │                  │ → CONFIG_LD
T3   │                  │ → MVIN (B tile 0 → Scratchpad bank)     [DMA 触发]
T10  │                  │ → MVIN (A tile 0 → Scratchpad bank)
T15  │                  │ → PRELOAD (B addr → PE Array c1/c2)    [ExecuteController]
T16  │                  │ → COMPUTE_AND_FLIP (A addr, C addr)    [MAC 开始]
T32  │                  │ → MVOUT (C acc → DRAM)                  [StoreController]
...  │                  │ 循环下一 tile ...

ROB 仲裁：LoadController 和 ExecuteController 可并行（流水线重叠 mvin 和 compute），由 ReservationStation 追踪 RAW
依赖（Controller.scala:425~440）。

---

1. 算子实现与内存设计

3.1 Im2Col 算子

源文件：Im2Col.scala

核心数据结构

// Im2Col.scala:8~27
class Im2ColReadReq:
addr: LocalAddr     // SPAD 起始地址 (bank + row 编码)
ocol: UInt(8.W)     // 输出列数
krow: UInt(4.W)     // 当前 kernel 行
icol, irow: UInt    // 输入 col/row 维度
stride, channel, row_turn, kdim2, row_left
im2col_cmd: Bool    // 是否激活 im2col 模式
weight_double_bank / weight_triple_bank: Bool

内存访问设计

Im2Col 使用两个滑动计数器模拟卷积窗口：

// Im2Col.scala:145~146
val window_address = WireInit(0.U)
window_address := window_start_counter + window_row_counter

- window_start_counter：水平方向偏移（输出列），每次步进 stride；换行时步进 kdim + (stride-1)*icol
- window_row_counter：垂直方向偏移（kernel 行），遍历 kernel_dim×kernel_dim 窗口内的行

SRAM 地址计算（Im2Col.scala:148~154）：
val im2col_spad_bank = (copy_addr + window_address).sp_bank()
val im2col_spad_row  = (copy_addr + window_address).sp_row()
io.sram_reads(im2col_spad_bank).req.bits.addr := im2col_spad_row
io.sram_reads(im2col_spad_bank).req.valid     := sram_read_req && !im2col_fin_reg

Bank Conflict 处理（Im2Col.scala:156~167）：

- 同时发起相邻 bank 读请求（bank, bank+1, bank-1），覆盖跨 bank 的宽 kernel
- 若 weight 占用 Bank 2（weight_double_bank），则禁止 Im2Col 访问 bank 2
- weight_triple_bank 进一步禁止 bank 1

数据复用：通过 spad_read_delay+1 深度的 Queue 对齐 SRAM 读延迟，避免重复读取同一 scratchpad 行（Im2Col.scala:136）。

Im2Col 5 状态 FSM（Im2Col.scala:104~289）

nothing_to_do ──[im2col_en]──► waiting_for_im2col
│ [start_inputting]
▼
preparing_im2col
│ [sram_resp_valid]
▼
doing_im2col ──[turn-1]──► preparing_im2col
│ [turn==0]
▼
im2col_done
│ [row_turn>0]
└──────────────► waiting_for_im2col
│ [row_turn==0]
└──────────────► nothing_to_do

输出向量（每轮 block_size=16 个元素）经 a_im2col 送回 ExecuteController。当 channel < 16 时，剩余位填充为 0（Im2Col.scala:411~423）。

3.2 GEMM 算子（Weight-Stationary 数据流）

源文件：PE.scala、Tile.scala

PE 内部结构

每个 PE 持有两个寄存器 c1、c2（双缓冲），交替用于 weight stationary：

// PE.scala:70~71
val c1 = Reg(cType)  // 当前 weight
val c2 = Reg(cType)  // 备用 weight（double buffering）

WS 模式下（PE.scala:118~130）：
Weight 存在 c1 或 c2（由 prop 控制）
in_a（激活）水平流入 → out_a 传递给右邻 PE
in_b（上方 PE 的部分和）垂直流入 → out_b = MAC(c_weight, in_a, in_b)
out_b 传递给下方 PE

when prop === PROPAGATE:
out_c := c1                      // 输出当前权重（preload 阶段）
out_b := MAC(c2, in_a, in_b)     // 用 c2 做 multiply-accumulate
c1 := d                          // 加载新权重

脉动阵列数据流

Tile.scala 中的数据流方向（16×16 PE）：

激活 A[row]  ────────────────────────────►  (水平广播/流动)
PE[0][0] → PE[0][1] → ... → PE[0][15]
↓           ↓                ↓
PE[1][0] → PE[1][1] → ... → PE[1][15]
↓           ↓                ↓
...
PE[15][0]→ PE[15][1]→ ... → PE[15][15]
↓           ↓                ↓
部分和 B 向下流动 → 最终 out_b 流出底部

权重 W (preload → c1/c2) 固定在每个 PE 内 (Weight Stationary)

Tile.scala:47~52 实现激活水平广播：
for (r <- 0 until rows) {
tile(r).foldLeft(io.in_a(r)) { case (in_a, pe) =>
pe.io.in_a := in_a; pe.io.out_a  // 链式传递
}
}

ExecuteController 中 PRELOAD + COMPUTE 序列

// ExecuteController.scala:82~88
val DoComputes = functs.map(f => f === COMPUTE_AND_FLIP_CMD || f === COMPUTE_AND_STAY_CMD)
val DoPreloads = functs.map(_ === PRELOAD_CMD)

- PRELOAD：将 Scratchpad 中的 B tile 送入 PE 阵列 c 寄存器（prop 翻转触发 flip）
- COMPUTE_AND_FLIP：激活流入，同时翻转 prop → 下一个 weight tile 可并行 preload
- COMPUTE_AND_STAY：激活继续累加，不翻转
---
总结对照表

┌──────────┬──────────────────────────────────────────────┬──────────────────────────────────┬──────────────────────────────────────────────┐
│   层次   │                     组件                     │              源文件              │                   关键机制                   │
├──────────┼──────────────────────────────────────────────┼──────────────────────────────────┼──────────────────────────────────────────────┤
│ 软件 ISA │ gemmini_mvin/mvout/preload/compute/loop_ws   │ gemmini.h                        │ RoCC 自定义指令宏                            │
├──────────┼──────────────────────────────────────────────┼──────────────────────────────────┼──────────────────────────────────────────────┤
│ 前端解码 │ LoopConv、LoopMatmul 硬件展开器              │ LoopConv.scala、LoopMatmul.scala │ 将高级 loop 指令展开为                       │
│          │                                              │                                  │ mvin/preload/compute/mvout 序列              │
├──────────┼──────────────────────────────────────────────┼──────────────────────────────────┼──────────────────────────────────────────────┤
│ 调度     │ ReservationStation (ROB)                     │ ReservationStation.scala         │ 追踪 ld/ex/st 间 RAW 依赖，仲裁三路并行流水  │
├──────────┼──────────────────────────────────────────────┼──────────────────────────────────┼──────────────────────────────────────────────┤
│ 内存     │ Scratchpad (256KB/4banks) + Accumulator      │ Scratchpad.scala                 │ TileLink DMA，单端口 SRAM，Im2Col            │
│          │ (64KB)                                       │                                  │ 共享读端口仲裁                               │
├──────────┼──────────────────────────────────────────────┼──────────────────────────────────┼──────────────────────────────────────────────┤
│ Im2Col   │ 5 状态 FSM，双计数器窗口寻址                 │ Im2Col.scala                     │ 直接访问 Scratchpad SRAM，bank 冲突由 weight │
│          │                                              │                                  │  bank 分离解决                               │
├──────────┼──────────────────────────────────────────────┼──────────────────────────────────┼──────────────────────────────────────────────┤
│ PE Array │ 16×16 PE，双缓冲寄存器                       │ PE.scala、Tile.scala             │ WS/OS 双模式，prop 信号控制 weight           │
│          │                                              │                                  │ 翻转与流出                                   │
├──────────┼──────────────────────────────────────────────┼──────────────────────────────────┼──────────────────────────────────────────────┤
│ TilerFSM │ 16 状态 CISC 模式调度                        │ TilerFSM.scala                   │ Output-Group 循环分块，最优 Accumulator 利用 │
└──────────┴──────────────────────────────────────────────┴──────────────────────────────────┴──────────────────────────────────────────────┘
```

### 指令架构

RoCC指令（MACRO）是Config指令+loop启动指令，loop在片上被unrolling成微指令。

微指令包含控制模块Load、Store、ExeCtrl的配置指令**Config**，DRAM和SRAN之间的DMA传输指令**MVIN和MVOUT**，SRAM和PE内缓存Stationary数据之间传输的**Prefetch**，PE阵列的计算指令**COMPUTE**。

io.dma.req是MVIN和MVOUT对应的scala描述结构，io.sram.read是SRAM到PE的scala描述结构。

```bash
Gemmini 指令系统深度剖析

  ---
  1. 指令粒度与编码

  双层指令体系（RISC micro + CISC macro）

  Gemmini 采用混合两级指令粒度设计，在同一 ISA 命名空间下共存：

  ┌───────────────────────────┬───────────────────────────────────────────┬─────────────────────────┬────────────────────────────────┐
  │           类型            │                 代表指令                  │          粒度           │            谁来执行            │
  ├───────────────────────────┼───────────────────────────────────────────┼─────────────────────────┼────────────────────────────────┤
  │ RISC 微指令（Low-level）  │ CONFIG / LOAD / PRELOAD / COMPUTE / STORE │ 单 tile（16×16 块）操作 │ 直接进 ReservationStation      │
  ├───────────────────────────┼───────────────────────────────────────────┼─────────────────────────┼────────────────────────────────┤
  │ CISC 宏指令（Loop-level） │ LOOP_WS / LOOP_CONV_WS + 6 条配置         │ 描述整个矩阵/卷积循环   │ LoopMatmul/LoopConv 硬件展开器 │
  └───────────────────────────┴───────────────────────────────────────────┴─────────────────────────┴────────────────────────────────┘

  指令定义（GemminiISA.scala）

  // GemminiISA.scala:8~39 — funct 字段（7-bit）完整分配
  val CONFIG_CMD         = 0.U   // 配置寄存器（LD/EX/ST/NORM 子类型）
  val LOAD2_CMD          = 1.U   // mvin — 权重矩阵 B
  val LOAD_CMD           = 2.U   // mvin — 激活矩阵 A
  val STORE_CMD          = 3.U   // mvout → DRAM
  val COMPUTE_AND_FLIP_CMD = 4.U // 计算 + 翻转 prop（新 weight preload 完成）
  val COMPUTE_AND_STAY_CMD = 5.U // 计算 + 不翻转（继续累加）
  val PRELOAD_CMD        = 6.U   // 权重 preload 到 PE 阵列
  val FLUSH_CMD          = 7.U   // TLB flush

  // CISC 宏指令（硬件展开）
  val LOOP_WS            = 8.U   // 触发 WS matmul 循环展开
  val LOOP_WS_CONFIG_BOUNDS  = 9.U   // I, J, K 上界 + pad
  val LOOP_WS_CONFIG_ADDRS_AB= 10.U  // A, B 基地址
  val LOOP_WS_CONFIG_ADDRS_DC= 11.U  // D, C 基地址
  val LOOP_WS_CONFIG_STRIDES_AB=12.U // A, B 行步长
  val LOOP_WS_CONFIG_STRIDES_DC=13.U // D, C 行步长

  val LOOP_CONV_WS       = 15.U  // 触发卷积循环展开（7 条配置 + 1 触发）

  关键指令字段结构

  LOAD 指令（MvinRs2，GemminiISA.scala:71~78）：

  rs1 [63:0] = DRAM 虚拟地址 (coreMaxAddrBits)
  rs2 [63:0] = | num_rows[15:0 高端] | num_cols[15:0] | local_addr[31:0] |
                │  tile 行数          │  tile 列数      │  Scratchpad 地址  │

  PRELOAD 指令（PreloadRs，GemminiISA.scala:231~238）：

  rs1 = | num_rows | num_cols | B_local_addr |  ← B tile 在 Scratchpad 中的位置
  rs2 = | num_rows | num_cols | C_local_addr |  ← C tile 在 Accumulator 中的写目标

  CONFIG_EX 指令（ConfigExRs1，GemminiISA.scala:203~215）：

  rs1 = | acc_scale[63:32] | a_stride[31:16] | b_transpose[9] | a_transpose[8] |
        | set_only_strides[7] | activation[4:3] | dataflow[2] | cmd_type[1:0] |
  rs2 = | c_stride[63:48] | relu6_shift[47:32] | in_shift[31:0] |

  LOOP_WS 触发指令（gemmini.h:366~374，6 条配置 + 1 触发）：

  LOOP_WS_CONFIG_BOUNDS: rs1=pad_{K,J,I}  rs2=bound_{K,J,I}  (各16-bit packed)
  LOOP_WS_CONFIG_ADDRS_AB: rs1=*A  rs2=*B
  LOOP_WS_CONFIG_ADDRS_DC: rs1=*D  rs2=*C
  LOOP_WS_CONFIG_STRIDES_AB: rs1=A_stride  rs2=B_stride
  LOOP_WS_CONFIG_STRIDES_DC: rs1=D_stride  rs2=C_stride
  LOOP_WS: rs1=flags(ex_accumulate,full_C,low_D,act,spad_ids) rs2=(is_resadd,B_trans,A_trans)

  ---
  2. 指令流与控制逻辑

  取指与分发路径（完整代码路径）

  CPU 发射 RoCC 指令
      │ io.cmd (RoCCCommand)                 Controller.scala:237
      ▼
  raw_cmd_q (Queue, depth=2)               Controller.scala:237~244
      │ Decoupled 握手
      ▼
  LoopConv (可选)                           Controller.scala:251~259
      │ conv_cmd：若 funct ∈ {LOOP_CONV_WS,...}，展开为微指令
      │ 否则透传
      ▼
  LoopMatmul                               Controller.scala:262~267
      │ loop_cmd：若 funct ∈ {LOOP_WS,...}，展开为微指令
      │ 否则透传
      ▼
  Queue(loop_cmd) → unrolled_cmd           Controller.scala:269
      │
      ▼ Controller.scala:508~514
  ReservationStation.io.alloc              Controller.scala:274~276
      │  分类到 ldq / exq / stq
      ▼
  ┌──────────────┬──────────────┬──────────────┐
  │  issue.ld    │  issue.ex    │  issue.st    │
  │ (8 entries)  │ (16 entries) │ (4 entries)  │
  └──────┬───────┴──────┬───────┴──────┬───────┘
         │              │              │
         ▼              ▼              ▼
  LoadController  ExecuteController  StoreController
         │              │              │
         ▼              ▼              ▼
    DMA (mvin)    PE Array Mesh    DMA (mvout)

  依赖检测与同步机制

  ReservationStation 实现了基于地址范围重叠的 Tomasulo 风格 ROB，不使用屏障指令，而是细粒度的位向量依赖追踪（ReservationStation.scala:108~112）：

  class Entry extends Bundle {
    val deps_ld = Vec(reservation_station_entries_ld, Bool()) // 依赖哪些 LD 项
    val deps_ex = Vec(reservation_station_entries_ex, Bool()) // 依赖哪些 EX 项
    val deps_st = Vec(reservation_station_entries_st, Bool()) // 依赖哪些 ST 项

    def ready(): Bool =
      !(deps_ld.reduce(_ || _) || deps_ex.reduce(_ || _) || deps_st.reduce(_ || _))
  }

  依赖类型识别（ReservationStation.scala:327~379）：

  ┌────────────────────────────┬────────────────────────────────────────────┬───────────────┐
  │         新指令类型         │                 检测的依赖                 │   冲突类型    │
  ├────────────────────────────┼────────────────────────────────────────────┼───────────────┤
  │ LOAD（mvin）               │ 在飞的 LD + EX（dst 重叠）+ ST（dst 重叠） │ WAR, WAW      │
  ├────────────────────────────┼────────────────────────────────────────────┼───────────────┤
  │ EXECUTE（preload/compute） │ 在飞的 LD（操作数重叠）+ ST（写后读）      │ RAW, WAR, WAW │
  ├────────────────────────────┼────────────────────────────────────────────┼───────────────┤
  │ STORE（mvout）             │ 在飞的 LD/EX 写入操作数                    │ RAW, WAW      │
  └────────────────────────────┴────────────────────────────────────────────┴───────────────┘

  地址范围表示为 (start, end) + wraps_around 标志，overlaps() 函数判断是否存在地址重叠（ReservationStation.scala:72~76）：

  def overlaps(other: OpT): Bool = {
    ((other.start <= start && (start < other.end || other.wraps_around)) ||
     (start <= other.start && (other.start < end || wraps_around))) &&
    !(start.is_garbage() || other.start.is_garbage())
  }

  清除依赖时机：
  1. 指令发射时（ReservationStation.scala:463~476）：同队列内的后续指令清除对刚发射指令的依赖（针对 CONFIG 等 complete-on-issue 指令）
  2. 指令完成时（ReservationStation.scala:492~523）：广播到所有项，清除对应 deps_ld/deps_ex/deps_st bit

  ---
  3. 指令调度与并发

  流水线并行（Compute/Communication Overlap）

  LoopMatmul 的核心机制是**先行约束（ld_ahead）**而非全局屏障（LoopMatmul.scala:458~463）：

  // Execute 子模块只有在 Load 进度"超前"时才发射
  val lda_ahead = io.lda_completed ||
    io.ld_ka > k || (io.ld_ka === k && io.ld_i > i)  // LdA 的 k 迭代已超过 Execute 当前 k
  val ldb_ahead = io.ldb_completed ||
    io.ld_kb > k || (io.ld_ka === k && io.ld_j > j)  // LdB 的 j 迭代已超过
  val ldd_ahead = io.ldd_completed

  val ld_ahead = lda_ahead && ldb_ahead && ldd_ahead
  io.cmd.valid := state =/= idle && !io.rob_overloaded && ld_ahead && !req.skip

  同理，StC 子模块追踪 Execute 进度（LoopMatmul.scala:627~634）：

  val ex_ahead = io.ex_completed ||
    (io.ex_k === req.max_k - 1.U &&
     (io.ex_j >= j + blocks || (io.ex_j === j + blocks - 1.U && io.ex_i > i)))
  io.cmd.valid := state =/= idle && !io.rob_overloaded && ex_ahead

  效果：LdA、LdB、Execute、StC 四条指令流并发发射，形成类似如下的流水线重叠：

  k=0: [LdA(k=0)] [LdB(k=0)]
  k=0:                         [EX(k=0)] （等 ld_ahead 满足后立即）
  k=1: [LdA(k=1)] [LdB(k=1)]  ←── 与 EX(k=0) 并行！
                                            [StC(i=0,j=0)] ←── 等 ex_ahead 满足
  k=1:                         [EX(k=1)]

  双缓冲 Ping-Pong 管理

  LoopMatmul 在硬件上维护 2 个并发 Loop，实现 Scratchpad 双缓冲（LoopMatmul.scala:912~917）：

  val concurrent_loops = 2
  val loops = Reg(Vec(concurrent_loops, new LoopMatmulState(...)))
  val head_loop_id = Reg(UInt(log2Up(concurrent_loops).W))
  val tail_loop_id = (~head_loop_id).asUInt  // 始终是 head 的取反
  val head_loop = loops(head_loop_id)   // 当前执行的 loop
  val tail_loop = loops(tail_loop_id)   // 正在配置的下一个 loop

  LoopMatmulState 中的地址分配实现 Ping-Pong（LoopMatmul.scala:866~867）：

  val a_addr_start = UInt(...)  // loop 0: spad[0..N/2], loop 1: spad[N/2..N]
  val b_addr_end   = UInt(...)  // B tile 从 sp 末端向前分配，避免与 A 重叠

  切换触发逻辑：all_completed() 为真时（LoopMatmul.scala:863）：

  def all_completed(): Bool =
    lda_completed && ldb_completed && ldd_completed && ex_completed && st_completed

  head 和 tail 互换，tail_loop 提前接收新的 LOOP_WS 配置，允许下一个 matmul 的 LdA/LdB 在上一个 EX 完成之前提前启动。

  ReservationStation 通过地址范围冲突检测自动阻止实际的数据竞争——在 A 未写完之前，相同地址的 COMPUTE 被依赖位卡住。

  多算子乱序调度（Out-of-Order Issue）

  LoopConv Unroller ─►─┐
                        └► LoopMatmul (concurrent_loops=2)
                                  │
                                  ▼
                      ┌──── 全局 5路 Arbiter ────┐
                      │ stC(prio=0) │ ex(prio=1)  │
                      │ ldD(prio=2) │ ldAB(prio=3)│
                      │ stC_spad(4) │             │
                      └─────────────────────────┘
                                  │
                                  ▼
                      ReservationStation (3 独立队列)
                      │ ldq(8) │ exq(16) │ stq(4) │
                           ↕            ↕
                      三路 OOO 发射（地址依赖保证正确性）

  不是串行等待：后一个算子的配置（LOOP_WS 6+1 条）在 head_loop 的 ex_completed 未到来之前就可以写入 tail_loop（LoopMatmul.scala:994）：

  cmd.ready := Mux(is_loop_cmd, !loop_being_configured.configured, !loop_configured && io.out.ready)
  // is_loop_cmd 为 true 时：tail_loop 未配置即可接受新配置，无需等 head_loop 完成

  ---
  4. 算子映射实操：WS GEMM 指令序列

  示例参数

  一个 3×3×3 tile 的矩阵乘法（I=3, J=3, K=3，每 tile 16×16，矩阵尺寸 48×48×48）：

  CPU 侧：发射 7 条 CISC 宏指令

  // gemmini.h:366 — gemmini_loop_ws 宏展开为 6 条配置 + 1 条触发
  ROCC(k_LOOP_WS_CONFIG_BOUNDS)   // rs1=pad_{3,3,3}   rs2={K=3,J=3,I=3}
  ROCC(k_LOOP_WS_CONFIG_ADDRS_AB) // rs1=*A  rs2=*B
  ROCC(k_LOOP_WS_CONFIG_ADDRS_DC) // rs1=*D  rs2=*C
  ROCC(k_LOOP_WS_CONFIG_STRIDES_AB)
  ROCC(k_LOOP_WS_CONFIG_STRIDES_DC)
  ROCC(k_LOOP_WS)                 // ← 触发硬件展开器

  硬件 LoopMatmul 展开为的微指令数量

  对于 I=3, J=3, K=3（假设无 bias，WS 模式）：

  ┌──────────────────────┬─────────────────────────────────────────────┬──────────────────────────────┐
  │      微指令类型      │                    数量                     │           循环维度           │
  ├──────────────────────┼─────────────────────────────────────────────┼──────────────────────────────┤
  │ CONFIG_EX            │ 1                                           │ 全局一次                     │
  ├──────────────────────┼─────────────────────────────────────────────┼──────────────────────────────┤
  │ MVIN（A） LOAD_CMD   │ I×K = 9                                     │ i∈[0,3), k∈[0,3)             │
  ├──────────────────────┼─────────────────────────────────────────────┼──────────────────────────────┤
  │ MVIN（B） LOAD2_CMD  │ K×J = 9                                     │ k∈[0,3), j∈[0,3)             │
  ├──────────────────────┼─────────────────────────────────────────────┼──────────────────────────────┤
  │ PRELOAD              │ I×J×K = 27（实际 K=0 时才发，否则 GARBAGE） │ k=0→实 preload, k>0→垃圾地址 │
  ├──────────────────────┼─────────────────────────────────────────────┼──────────────────────────────┤
  │ COMPUTE_AND_FLIP     │ I×J×K = 27                                  │ 每 (i,j,k) 一条              │
  ├──────────────────────┼─────────────────────────────────────────────┼──────────────────────────────┤
  │ MVOUT（C） STORE_CMD │ I×J = 9                                     │ i,j遍历                      │
  ├──────────────────────┼─────────────────────────────────────────────┼──────────────────────────────┤
  │ 总计                 │ 1 + 9 + 9 + 27 + 27 + 9 = 82 条微指令       │                              │
  └──────────────────────┴─────────────────────────────────────────────┴──────────────────────────────┘

  ▎ 注：PRELOAD 中，当 i===0 时 local_addr = b_addr（真实地址），否则 local_addr = GARBAGE_ADDR（跳过权重更新，复用上一个 weight）。这是 WS
  的关键优化（LoopMatmul.scala:420~422）。

  关键时间线（3×3×3 tile 矩阵，简化版）

  周期  LdA 子模块     LdB 子模块     Execute 子模块      StC 子模块
  ────  ─────────────  ─────────────  ──────────────────  ─────────────
    0   CONFIG_EX ────────────────────────────────────────────────────►
    1   MVIN A(0,0)   MVIN B(0,0)
    2   MVIN A(0,1)   MVIN B(0,1)
    3   MVIN A(0,2)   MVIN B(0,2)
    4   MVIN A(1,0)   MVIN B(1,0)    [ld_ahead: ka≥0,i≥0 ✓]
    5   MVIN A(1,1)   MVIN B(1,1)    PRELOAD B(0,0)→PE   │
    6   MVIN A(1,2)   MVIN B(1,2)    COMPUTE A(0,0)*B←   │
    7   MVIN A(2,0)   MVIN B(2,0)    PRELOAD GARB(1,0)   │
    8   MVIN A(2,1)   MVIN B(2,1)    COMPUTE A(1,0)*    [ex_ahead ✓]
    9   MVIN A(2,2)   MVIN B(2,2)    PRELOAD GARB(2,0)   MVOUT C(0,0)
   10   ─── 完成 ───   ─── 完成 ───   COMPUTE A(2,0)*     MVOUT C(0,1)
   11                               PRELOAD B(0,1)→PE    MVOUT C(0,2)
   12                               COMPUTE A(0,1)*      ─── ...  ───
   ...
   ~40                               ─── 完成 ───          MVOUT C(2,2)

  关键重叠：
  - LdA / LdB 在 T=1 启动，相互通过 WeightedArbiter 仲裁共享 DMA
  - Execute 在 ld_ahead 满足（ka>k 或 ka==k 且 ld_i>ex_i）后立即启动，不等 ld 全部完成
  - StC 在 ex_ahead（ex_k==max_k-1 且 ex_j>st_j）时提前启动

  WS 模式下 PRELOAD 的 Ping-Pong 逻辑

  k=0, j=0: PRELOAD B(0,0) → c1  [i=0时用真实地址]
  k=0, j=0: COMPUTE A(0,0)      [prop=PROPAGATE → 使用 c1]
  k=0, j=0: COMPUTE A(1,0)      [prop=STAY → c1不变]（i遍历用 COMPUTE_AND_STAY）
  k=0, j=0: COMPUTE A(2,0)

  k=1, j=0: PRELOAD GARBAGE     [i>0 → 不换 weight，c2也不变]
  k=1, j=0: COMPUTE A(0,1)      [COMPUTE_AND_FLIP → 同时翻转 prop，下次用 c2]
                                  ↑ 此时 c2=c1（因为 prop 翻转前已通过 d 赋值）

  ---
  核心类名总结

  ┌───────────────────────────────────┬───────────────────────────────────────────────┬──────────────────────────┐
  │             功能角色              │                     类名                      │          源文件          │
  ├───────────────────────────────────┼───────────────────────────────────────────────┼──────────────────────────┤
  │ 顶层调度器（Scheduler）           │ GemminiModule                                 │ Controller.scala         │
  ├───────────────────────────────────┼───────────────────────────────────────────────┼──────────────────────────┤
  │ CISC→RISC 展开器（Loop Unroller） │ LoopMatmul                                    │ LoopMatmul.scala         │
  ├───────────────────────────────────┼───────────────────────────────────────────────┼──────────────────────────┤
  │ 卷积展开器                        │ LoopConv                                      │ LoopConv.scala           │
  ├───────────────────────────────────┼───────────────────────────────────────────────┼──────────────────────────┤
  │ Load 微指令生成                   │ LoopMatmulLdA / LoopMatmulLdB / LoopMatmulLdD │ LoopMatmul.scala         │
  ├───────────────────────────────────┼───────────────────────────────────────────────┼──────────────────────────┤
  │ Execute 微指令生成                │ LoopMatmulExecute                             │ LoopMatmul.scala         │
  ├───────────────────────────────────┼───────────────────────────────────────────────┼──────────────────────────┤
  │ Store 微指令生成                  │ LoopMatmulStC / LoopMatmulStCSpad             │ LoopMatmul.scala         │
  ├───────────────────────────────────┼───────────────────────────────────────────────┼──────────────────────────┤
  │ 发射仲裁器（Dispatcher）          │ ReservationStation                            │ ReservationStation.scala │
  ├───────────────────────────────────┼───────────────────────────────────────────────┼──────────────────────────┤
  │ 地址依赖分析                      │ Entry.overlaps() / Entry.ready()              │ ReservationStation.scala │
  ├───────────────────────────────────┼───────────────────────────────────────────────┼──────────────────────────┤
  │ LD/EX 共享仲裁                    │ WeightedArbiter                               │ WeightedArbiter.scala    │
  ├───────────────────────────────────┼───────────────────────────────────────────────┼──────────────────────────┤
  │ Load 控制器                       │ LoadController                                │ LoadController.scala     │
  ├───────────────────────────────────┼───────────────────────────────────────────────┼──────────────────────────┤
  │ Execute 控制器                    │ ExecuteController                             │ ExecuteController.scala  │
  ├───────────────────────────────────┼───────────────────────────────────────────────┼──────────────────────────┤
  │ Store 控制器                      │ StoreController                               │ StoreController.scala    │
  └───────────────────────────────────┴───────────────────────────────────────────────┴──────────────────────────┘
```

### im2col下的Conv

im2col实现分为CPU完成，通过CPU读取DRAM、重排、写回DRAM，每层计算时输入和权重地址是分段连续，但每层执行引入CPU的性能瓶颈（同步），减少overlap机会。

im2col模块完成，将CPU控制重排的逻辑固化为硬件模块，运行时生成地址读取DRAM到SRAM，生成地址读取SRAM到PE。

window-row-counter和我设置的conv-com-ctrl中地址生成单元逻辑相同，区别在于gemmini中并行方式没有col或row的并行度，只有ci和co的并行度（WS），因此每个Bank row是一个输入位置的若干输入通道数据。

```bash
第一部分：路径 A — 软件 im2col + 通用矩阵乘

  ---
  A-1. 全链路调用栈（CPU Side）

  resnet50.c:main()
  │  conv=false 分支，第一层 conv_1
  │
  ├─① CPU im2col（gemmini_nn.h:304）
  │     im2col(
  │       batch_size=1, in_channels=3,
  │       in_row_dim=224, in_col_dim=224,
  │       I=conv_1_params.I,          ← 输出 patches 总数
  │       K=conv_1_params.K,          ← 每个 patch 的元素数
  │       images,                     ← 输入原始图像（NHWC）
  │       conv_1_in,                  ← 输出缓冲区（线性矩阵）
  │       &conv_1_params              ← 包含 stride/padding/kernel_size
  │     )
  │     ↓ 纯 CPU 三重循环（gemmini_nn.h:310~337）
  │     for(n_batch) for(im_row,-pad...) for(im_col,-pad...)
  │       for(filter_row) for(filter_col) for(im_channel)
  │         output[patch_row][patch_col] =
  │           input[n][pixel_row][pixel_col][c]  ← 直接内存复制
  │     ↓ 完成后 conv_1_in[I][K] 已是线性布局
  │
  └─② tiled_matmul_nn_auto（gemmini_nn.h:127）
        tiled_matmul_nn_auto(
          conv_1_params.I, conv_1_params.J, conv_1_params.K,
          conv_1_in,   ← im2col 输出缓冲区（A 矩阵）
          conv_1_w,    ← 权重（B 矩阵，已线性存储）
          conv_1_b,    ← 偏置（D 矩阵）
          conv_1_out,  ← 输出（C 矩阵）
          RELU, conv_1_params.output_scale, true,
          WS, false, "conv_1"
        )
        ↓
        tiled_matmul_auto(dim_I, dim_J, dim_K, A, B, D, C,
            dim_K, dim_J, dim_J, dim_J, ...)    ← stride 参数
        ↓
        tiled_matmul_outer(...)                  gemmini.h:716
        │   ┌─ 计算分块因子 tile_I, tile_J, tile_K
        │   ├─ 计算 I0, J0, K0（超块数量）
        │   └─ 选择内层函数指针 inner = &sp_tiled_matmul_ws
        │
        for(i0) for(j0) for(k0):
          (*inner)(a_ptr, b_ptr, D_ptr, C_ptr, I, J, K, pad_I, pad_J, pad_K, ...)
          ↓
          sp_tiled_matmul_ws(...)               gemmini.h:526
          └─ gemmini_loop_ws(I, J, K, ...)      gemmini.h:708
                ↓ 展开为 6+1 条 RoCC CISC 宏指令（见 A-3）

  A-2. 卷积超参数 → GEMM I, J, K 的转换点

  转换发生在 gemmini_nn.h 的 ConvParams 结构体（第 12~35 行），在 resnet50_params.h 中静态计算完成：

  // gemmini_nn.h:34
  struct ConvParams {
      int I, J, K;   ← 这里预计算好
  };

  // 映射关系（物理含义）：
  I = batch_size × out_row_dim × out_col_dim
      // = 1 × 112 × 112 = 12544  ← 输出特征图所有空间位置（patch 数量）

  J = out_channels
      // = 64                      ← 输出通道数（GEMM 列维度）

  K = kernel_size × kernel_size × in_channels
      // = 7 × 7 × 3 = 147        ← 每个 patch 展开后的宽度（GEMM 内积维度）

  这一转换是纯静态离线完成的，在 tiled_matmul_nn_auto() 调用处直接传入 conv_1_params.I, J,
  K（resnet50.c:85~88），没有任何运行时计算卷积维度的逻辑。

  A-3. RoCC 指令序列（ISA Layer）

  sp_tiled_matmul_ws() → gemmini_loop_ws() 宏（gemmini.h:708 调用 gemmini.h:366）展开为：

  ① ROCC(k_LOOP_WS_CONFIG_BOUNDS)   // rs2={K, J, I}  rs1={pad_K, pad_J, pad_I}
  ② ROCC(k_LOOP_WS_CONFIG_ADDRS_AB) // rs1=*A(conv_1_in)  rs2=*B(conv_1_w)
  ③ ROCC(k_LOOP_WS_CONFIG_ADDRS_DC) // rs1=*D(conv_1_b)   rs2=*C(conv_1_out)
  ④ ROCC(k_LOOP_WS_CONFIG_STRIDES_AB)// rs1=A_stride=dim_K  rs2=B_stride=dim_J
  ⑤ ROCC(k_LOOP_WS_CONFIG_STRIDES_DC)// rs1=D_stride=dim_J  rs2=C_stride=dim_J
  ⑥ ROCC(k_LOOP_WS)                  // rs1=flags(ex_accumulate=1,full_C=0,low_D=0)

  关键字段证明 im2col 路径不触发 im2col 硬件：

  - k_LOOP_WS 指令中 不含 weight_stride 字段，该字段仅存在于 LOOP_CONV_WS 中
  - LoopMatmul 硬件展开器不会设置 weight_stride 寄存器
  - ExecuteController.scala:136 中 im2col_en 的定义：

  // ExecuteController.scala:136
  val im2col_en = config.hasIm2Col.B && weight_stride =/= 0.U

  由于路径 A 中 weight_stride 寄存器永远为 0（从未被 LOOP_WS 系列指令写入），im2col_en 始终为 false。

  A-4. 微指令到架构完成（Hardware Side）

  4a. ReservationStation 状态变化

  k_LOOP_WS 触发 LoopMatmul 硬件展开，产生如下微指令流入 ReservationStation：

  微指令          → 分入队列  → Entry 初始状态
  ─────────────────────────────────────────────────────────
  CONFIG_EX       → exq       → is_config=true, complete_on_issue=false
  CONFIG_LD(×3)  → ldq       → is_config=true, complete_on_issue=true
  MVIN A(i,k)    → ldq       → opa={spad_addr_A..A+rows}, deps_ex={当前ex队列}
  MVIN B(k,j)    → ldq       → opa={spad_addr_B..B+rows}, deps_ex={当前ex队列}
  PRELOAD(k,j,i) → exq       → opa=B_spad, opb=C_acc, deps_ld={对应MVIN B}
  COMPUTE(i,j,k) → exq       → opa=A_spad, opb=C_acc, deps_ld={对应MVIN A}
  MVOUT C(i,j)   → stq       → opb=C_acc, deps_ex={对应COMPUTE}

  发射时（ReservationStation.scala:438）：
  val issue_valids = entries_type.map(e => e.valid && e.bits.ready() && !e.bits.issued)
  val issue_sel = PriorityEncoderOH(issue_valids)  // 最低 index 优先

  4b. LoadController 的 DMA 访存（线性地址证明）

  LoadController.scala:101 中的地址计算：

  // LoadController.scala:101~102
  io.dma.req.bits.vaddr := vaddr + row_counter * stride
  io.dma.req.bits.laddr := localaddr_plus_row_counter

  其中：
  - vaddr = cmd.bits.cmd.rs1 = A + (i*A_row_stride + k)*DIM （由 LoopMatmulLdA 计算，LoopMatmul.scala:65）
  - stride = 由 CONFIG_LD 设置的 A_row_stride * sizeof(elem_t) = K*DIM*1 字节
  - row_counter 在 sending_rows 状态下线性递增（LoadController.scala:134）

  地址序列为严格线性：addr[row] = A_base + row * K*DIM，无任何非线性跳转或窗口滑动逻辑。

  三状态 FSM（LoadController.scala:140~171）：
  waiting_for_command
    → [DoLoad && alloc.ready] → waiting_for_dma_req_ready
    → [req.fire]              → sending_rows
    → [last_row]              → waiting_for_command + cmd.ready := true（完成）

  4c. 计算到写回（PE 阵列全生命周期）

                                            ExecuteController 时序
  PRELOAD 发射
    │ start_inputting_b=true
    │ srams.read(dataBbank).req ──────────────────────► Scratchpad SRAM
    │                                                        │
    │ [spad_read_delay 周期后]                               ▼
    │ srams.read(dataBbank).resp.bits.data ◄── b_row = Weight[k][j]
    │
    │ mesh.io.b.bits ← b_row（经 d_fire_counter 逐行送入 PE 阵列）
    │ mesh.io.req.bits.pe_control.propagate ← in_prop（FLIP=新weight，STAY=累加）
    │
    └─ [约 block_size=16 周期后] 权重填充完成

  COMPUTE 发射（im2col_en=false 分支，ExecuteController.scala:442~446）
    │ io.srams.read(dataAbank).req.bits.addr
    │     := a_address.sp_row()  ← 纯线性：A_base + a_fire_counter（无滑窗）
    │ start_inputting_a=true
    │
    │ [spad_read_delay 周期后]
    │ srams.read(dataAbank).resp.bits.data → activation row
    │
    │ mesh.io.a.bits ← activation_row
    │
    └─ PE 阵列内部（PE.scala WS 模式）：
         MAC: out_b[col] += c_weight[col] * in_a
         out_b 向下流动 block_size 行后 → mesh.io.resp

  mesh.io.resp（MeshWithDelays 输出）
    │ result[i][j] = Σ_k(A[i][k] * B[k][j])
    │
    │ acc.write[acc_bank].bits.addr = c_acc_addr
    │ acc.write.bits.data = result
    └─► Accumulator SRAM 写入（32-bit 精度累加）

  MVOUT 发射（COMPUTE 完成后，ex_ahead 满足）
    │ StoreController 发射 STORE_CMD
    │ dma.req.bits.vaddr = C_dram_addr
    │ dma.req.bits.laddr = C_sp_addr（acc 地址，is_acc_addr=true）
    │ 激活函数（RELU）+ 量化（acc_scale）在 Scratchpad Accumulator 读出时施加
    │ acc.read_resp → DMA → DRAM（C = ReLU(A×B + D)）
    └─► io.completed → ReservationStation.io.completed（清除 deps_st bits）

  ---
  第二部分：路径 B — 全硬件专用卷积

  ---
  B-1. 全链路调用栈（CPU Side）

  resnet50.c:main()
  │  conv=true 分支（默认）
  │
  └─ tiled_conv_auto(                              resnet50.c:108
         batch_size=1, in_row=224, in_col=224,
         in_channels=3, out_channels=64,
         out_row=112, out_col=112,
         stride=2, input_dilation=1,
         kernel_dilation=1, padding=3, kernel_dim=7,
         false, false, false, false, false,         ← 各种 trans 标志
         images, conv_1_w, conv_1_b, conv_1_out_pooled,
         RELU, output_scale,
         pool_size=3, pool_stride=2, pool_padding=1,
         WS                                         ← tiled_conv_type
     )                                              gemmini.h:2860
     ↓
     tiled_conv_stride_auto(                        gemmini.h:2836
         // 自动计算 in_stride, weight_stride, out_stride
         in_stride = in_channels = 3
         weight_stride = out_channels = 64
         out_stride = out_channels = 64
     )
     ↓
     tiled_conv(                                    gemmini.h:2194
         // 自动计算分块因子：
         // batches, porows, pocols, pochs, krows, kcols, kchs
         // 以及 lpad, rpad, upad, dpad（padding 分解）
         ├─ gemmini_extended_config_ex(WS, RELU, ...)
         └─ sp_tiled_conv(...)                      gemmini.h:1374
            │
            │ ★ 卷积参数在此原封不动传递，不做任何数据重排 ★
            │
            └─ gemmini_loop_conv_ws(                gemmini.h:384~401
                   batch_size=1, in_row=224, in_col=224,
                   in_channels=3, out_channels=64,
                   stride=2, padding=3, kernel_dim=7,
                   batches=1, porows=tile, pocols=tile,
                   pochs=tile, krows=7, kcols=7, kchs=3,
                   lpad=3, rpad=3, upad=3, dpad=3,
                   weights=conv_1_w, output=conv_1_out_pooled,
                   bias=conv_1_b, input=images,
                   ...
               )
               → 7 条 CONFIG + 1 条 LOOP_CONV_WS RoCC 指令

  Stride、Padding 直透证明：sp_tiled_conv() 在 gemmini.h:1465 直接调用：

  gemmini_loop_conv_ws(
      batch_size, in_row_dim, in_col_dim, in_channels,
      out_channels, out_row_dim, out_col_dim,
      ..., stride, padding, kernel_dim, kernel_dilation, ...
      weights, output, bias, input, ...
  );

  stride 和 padding 以原始整数直接塞入 LOOP_CONV_WS_CONFIG_1 的 rs2 字段（gemmini.h:387），不经任何展开或重排。

  B-2. LOOP_CONV_WS 字段编码与计数器初始化

  7 条配置指令字段编码

  CONFIG_1 rs1=[out_channels:48|in_channels:32|in_row_dim:16|batch_size:0]
           rs2=[padding:56    |stride:48      |out_col_dim:32|pool_out_row:16|out_row_dim:0]

  CONFIG_2 rs1=[kernel_dim:48|pool_out_col:32|pool_size:16|pool_stride:8|pool_pad:0]
           rs2=[batches:48   |porows:32      |pocols:16   |pochs:0         ]

  CONFIG_3 rs1=[krows:48|kcols:32|kchs:16|lpad:0]
           rs2=[rpad:48 |upad:32 |dpad:24|plpad:16|in_col_dim:0]

  CONFIG_4 rs1=[orows:48|prad:32|pupad:21|pdpad:10|kernel_dilation:0]
           rs2=[in_stride:48|weight_stride:32|out_stride:16|ocols:0]

  CONFIG_5 rs1=*weights   rs2=*output
  CONFIG_6 rs1=*bias      rs2=*input

  LOOP_CONV_WS rs1=[a_spad_id:19|b_spad_id:17|max_pixels_per_row:8|dw:6|
                    trans_input:5|trans_w0132:4|trans_w1203:3|trans_out:2|
                    wrot180:1   |no_bias:0]
                rs2=[activation:4|input_dilated:2|downsample:1|no_pool:0]

  LoopConv 硬件接收配置（LoopConv.scala:1279~1376）

  当 LOOP_CONV_WS 到达时，loop_being_configured.configured := true.B（第 1371 行），触发 4 个并发子模块启动：

  // LoopConv.scala 内 7 层嵌套计数器初始化（以 LoopConvEx 为例）
  // 6 个外层迭代变量：b, porow, pocol, och, krow, kcol (LoopConv执行子模块)
  // 最内层 kch 由 im2col 硬件处理

  子模块          迭代变量                初始值  上界
  ─────────────────────────────────────────────────
  LoopConvLdBias  b, orow, ocol, och     全0    batches/orows/ocols/ochs
  LoopConvLdInput b, irow, icol, ich     全0    batches/irows/icols/ichs
  LoopConvLdWeights b, och, krow, kcol, kch 全0 batches/pochs/krows/kcols/kchs
  LoopConvEx      b, orow, ocol, och, krow, kcol 全0 (7层展开)
  LoopConvSt      b, porow, pocol, och   全0

  LoopConvEx 在 LoopConv.scala 约 850 行处定义，其 7 层嵌套按如下顺序展开（外→内）：

  batch(b) → pool_out_row(porow) → pool_out_col(pocol) →
    out_channel(och) → kernel_row(krow) → kernel_col(kcol) →
      [im2col 硬件处理 kernel_channel(kch) + input 窗口滑动]

  B-3. AGU 激活与 Im2Col FSM 信号链

  im2col_en 激活路径

  LoopConvEx 发射 PRELOAD + COMPUTE 指令
      │ cmd.rs2 中 weight_stride 字段已编码（由 CONFIG_4 rs2[47:32] 设置）
      │
      → ExecuteController.io.cmd 接收
      │
      → 在 CONFIG_CMD 处理中（ExecuteController.scala:568）：
      │   ocol   := cmd.bits(0).cmd.rs2(63,56)   ← 输出列数
      │   kdim2  := cmd.bits(0).cmd.rs2(55,48)   ← kernel_dim²
      │   weight_stride := cmd.bits(0).cmd.rs2(7,5) ← 卷积 stride
      │
      → ExecuteController.scala:136:
      │   val im2col_en = config.hasIm2Col.B && weight_stride =/= 0.U
      │              ↑ 当 weight_stride=2(conv1)时，im2col_en=true ✓
      │
      → ExecuteController.scala:504~529（Im2Col reads 块）：
      │   val read_a = a_valid && start_inputting_a &&
      │                !multiply_garbage &&
      │                im2col_wire && im2col_en   ← 此时为 true
      │
      → io.im2col.req.valid := read_a
      │   io.im2col.req.bits.addr   := a_address_rs1  ← SPAD 基地址
      │   io.im2col.req.bits.stride := weight_stride
      │   io.im2col.req.bits.krow   := krow
      │   io.im2col.req.bits.ocol   := ocol
      │   io.im2col.req.bits.im2col_cmd := im2col_en  ← true
      │
      → Im2Col.io.req.ready := busy（Im2Col.scala:172）
          ↑ 触发 Im2Col FSM

  同时，SRAM 直接读 A 的通路被关闭（ExecuteController.scala:424）：

  val read_a = a_valid && !a_read_from_acc && ... && !(im2col_wire && im2col_en)
  //                                                      ↑ im2col_en=true 时此项为 false

  Im2Col FSM 在 doing_im2col 状态下的物理地址生成

  滑动窗口非线性寻址的核心逻辑（Im2Col.scala:145~153）：

  // Im2Col.scala:145~154
  val window_address = WireInit(0.U)
  window_address := window_start_counter + window_row_counter
  //               ↑ 水平偏移（输出列）  ↑ 垂直偏移（kernel 行内）

  val im2col_spad_bank = (copy_addr + window_address).sp_bank()
  val im2col_spad_row  = (copy_addr + window_address).sp_row()
  io.sram_reads(im2col_spad_bank).req.bits.addr := im2col_spad_row
  io.sram_reads(im2col_spad_bank).req.valid :=
      sram_read_req && !im2col_fin_reg

  两个计数器的更新逻辑（Im2Col.scala:346~378）：

  window_row_counter（kernel 行内垂直偏移，单位：SPAD 行）
    ├─ 每次 im2col_fin_reg 时更新：
    │   if 未完成一行 kernel：+= 1（下移一行，不跨 kernel 行边界）
    │   if 跨 kernel 行边界：  += icol - kdim + 1（跨行跳转）
    │       → 对应输入特征图中 (kernel_row+1, 0) 的 SPAD 偏移
    └─ if 所有 channel 完成：  = 0（重置到窗口起点）

  window_start_counter（输出列方向水平偏移，单位：SPAD 行）
    ├─ sram_resp_valid 时（Im2Col.scala:350~354）：
    │   未换行：+= stride（水平滑动 stride 步）
    │   换行时：+= kdim + (stride-1)*icol（跳到下一行起点）
    │       = kernel_width + (stride-1)*input_col_width
    │       → 实现 (output_row+1, 0) 的 SPAD 偏移
    └─ 每输出 block_size 列（column_counter回绕）：
         window_start_counter := window_start_counter_prev（从保存点恢复）

  具体数值示例（conv_1: stride=2, kernel=7, in_col=224, block_size=16）：

  输出位置 (out_row=0, out_col=0): window_start=0, window_row=0
    → SPAD 行 = copy_addr + 0 + 0 = A_base[输入行0]

  输出位置 (out_row=0, out_col=1): window_start=stride=2, window_row=0
    → SPAD 行 = A_base + 2       = A_base[输入列2]（stride=2）

  输出位置换行 (out_row=1, out_col=0):
    window_start += kdim + (stride-1)*icol = 7 + 1*224 = 231
    → SPAD 行 = A_base + 231     = A_base[输入行2]（stride=2行跳转）

  kernel_row 迭代 (krow=1):
    window_row += 1 → 当前窗口下移一行
    → SPAD 行 = A_base + window_start + 1（读取输入行+1）

  这正是 非线性寻址的本质：相邻输出元素对应的输入 SPAD 地址步长为 stride，而不是 1。

  B-4. 架构收敛：端到端信号流

  ──────────────────────────────────────────────────────────────────────
                Im2Col 单元                ExecuteController
  ──────────────────────────────────────────────────────────────────────
  T=0  doing_im2col 状态
       sram_reads[bank].req.valid=true
       sram_reads[bank].req.bits.addr=im2col_spad_row

  T=spad_read_delay(≈2)
       sram_reads[bank].resp.valid=true
       sram_req_output = resp.bits.data.asTypeOf(Vec(block_size, inputType))
       im2col_data[0..15] = sram_req_output[0..channel-1]
                              (channel < 16 时剩余填 0)   Im2Col.scala:410~424
       valid_reg := true
       io.resp.bits.a_im2col = im2col_data    ← 16 个 int8 元素

  T=spad_read_delay+1
       ExecuteController:
       io.im2col.resp.ready = mesh.io.a.ready
       io.im2col.resp 触发 fire

       mesh.io.a.valid := true
       mesh.io.a.bits[row] := a_im2col[row]  ← 激活向量送入脉动阵列 A 口
       mesh.io.a.bits 按行逐拍送入 (block_size 行)

  T=spad_read_delay+1 ~ +block_size
       mesh.io.b (权重): 从 Scratchpad 直接读（非 im2col 路径）
       每周期：PE[r][c].out_b += weight[c] * activation[r]

  T≈spad_read_delay+block_size+mesh_latency
       MeshWithDelays 输出 resp.bits.data[c] = Σ_r(A[r]*W[r][c])
       → ExecuteController 收集 resp
       → 写入 Accumulator:
            acc.write[acc_bank].valid := true
            acc.write.bits.addr := c_acc_addr
            acc.write.bits.data := resp.bits.data + (上一轮结果，accumulate模式)
            acc.write.bits.acc  := accumulate || k ≠ 0

           [kch 维度全部完成后（all k done）]
           acc.write.bits.acc := false（覆盖写，清空旧值）

  StoreController 触发（ex_ahead 满足）：
       dma.req.bits.vaddr = output_dram_addr
       dma.req.bits.laddr = C_sp_addr（is_acc_addr=true）
       Scratchpad 读 Accumulator:
         acc.read_req[bank].bits.act := RELU
         acc.read_req[bank].bits.scale := acc_scale
         → AccumulatorScale 模块执行：
             result = clamp(ReLU(data * acc_scale), elem_t_min, elem_t_max)
         → 量化后 int8 数据送出 DMA

       io.dma.req.fire → ScratchpadMemReadRequest
       → TileLink DMA → L2 Cache → DRAM
         vaddr = C_out + (i*out_row*out_col + j) * out_channels
       完成后：io.dma.resp → cmd_tracker → io.completed → ReservationStation
       → 清除所有依赖此 rob_id 的 deps_st bits
       → conv_st_completed ++ → LoopConv st 子模块前进迭代计数器
  ──────────────────────────────────────────────────────────────────────

  ---
  两条路径对比总表

  ┌──────────────────┬────────────────────────────────────────────────────┬──────────────────────────────────────────────────┐
  │       维度       │            路径 A（软件 im2col + GEMM）            │           路径 B（硬件 LOOP_CONV_WS）            │
  ├──────────────────┼────────────────────────────────────────────────────┼──────────────────────────────────────────────────┤
  │ im2col 执行者    │ RISC-V CPU（gemmini_nn.h:304，纯 C 三重循环）      │ Im2Col 硬件单元（Im2Col.scala）                  │
  ├──────────────────┼────────────────────────────────────────────────────┼──────────────────────────────────────────────────┤
  │ im2col_en 信号   │ 始终 false（weight_stride==0）                     │ true（weight_stride≠0，由 CONFIG_IM2COL 写入）   │
  ├──────────────────┼────────────────────────────────────────────────────┼──────────────────────────────────────────────────┤
  │ 输入数据重排     │ 显式复制到 conv_1_in[] 缓冲区                      │ 零复制，直接从 SPAD 原始布局窗口寻址             │
  ├──────────────────┼────────────────────────────────────────────────────┼──────────────────────────────────────────────────┤
  │ A 矩阵 SRAM 读取 │ ExecuteController 直接线性读（a_address.sp_row()） │ Im2Col 单元滑动窗口读（window_start+window_row） │
  ├──────────────────┼────────────────────────────────────────────────────┼──────────────────────────────────────────────────┤
  │ DRAM→SPAD 加载   │ 线性 mvin（LoadController，步长=K*DIM）            │ 分块 mvin（LoopConvLdInput，带 padding 检测）    │
  ├──────────────────┼────────────────────────────────────────────────────┼──────────────────────────────────────────────────┤
  │ CPU 侧指令总数   │ 6+1=7 条 CISC（LOOP_WS）×超块数                    │ 6+1=7 条 CISC（LOOP_CONV_WS）×1                  │
  ├──────────────────┼────────────────────────────────────────────────────┼──────────────────────────────────────────────────┤
  │ 卷积参数传递     │ 离线转换为 I,J,K（ConvParams 静态字段）            │ 原始 stride/padding/kernel_dim 直透指令字段      │
  ├──────────────────┼────────────────────────────────────────────────────┼──────────────────────────────────────────────────┤
  │ 缓冲区消耗       │ 额外 I×K = patch数×patch宽 字节（CPU DRAM）        │ 0（不产生中间数据）                              │
  ├──────────────────┼────────────────────────────────────────────────────┼──────────────────────────────────────────────────┤
  │ 延迟特性         │ 更高（CPU im2col 串行 + GEMM 并行）                │ 更低（im2col 与 GEMM 硬件 pipeline）             │
  └──────────────────┴────────────────────────────────────────────────────┴──────────────────────────────────────────────────┘
```

### 端到端执行的资源分配

NHWC的layout，layer-by-layer的静态分配内存，覆盖使用SRAM，静态tiling。

```bash
Gemmini 端到端内存分配、地址空间管理与数据流转深度分析
                                                                                          
  ---
  一、全局内存规划与 Layer 间数据流转                                                                                                             
                                     
  1.1 DRAM 张量布局                                                                                                                               
                                                                                                                                                  
  Gemmini 不拥有任何"全局内存管理器"——所有 Layer 的输入/输出张量在 CPU 端以普通 C 数组形式静态分配于 DRAM（或栈/堆上）。resnet50.c 中的典型模式： 
                                                                                                                                                  
  int8_t conv_1_out[CONV_1_OUT_DIM * CONV_1_OUT_DIM * CONV_1_OUT_CHANNELS];                                                                       
  int8_t conv_2_out[CONV_2_OUT_DIM * CONV_2_OUT_DIM * CONV_2_OUT_CHANNELS];                                                                       
  
  Layer 间数据传递的本质是指针转移：前一层输出数组的地址直接作为下一层输入数组的虚地址传入 tiled_conv_auto() 或 tiled_matmul_auto()。没有显式     
  copy、没有格式转换，Gemmini DMA 引擎直接从该地址读取原始 NHWC（或行主序）布局数据。
                                                                                                                                                  
  1.2 DRAM 数据布局约定                                                                                                                           
  
  - 激活张量 (A/Input)：NHWC 行主序，[N][H][W][C]，int8                                                                                           
  - 权重张量 (B/Weight)：[O][KH][KW][I]（OHWI），int8（对卷积）；[K][N] 转置（对矩阵乘）
  - 偏置/累加器输出：int32，4字节对齐                                                                                                             
  - DMA stride 参数：软件显式传入 in_row_stride（= W×C）、weight_row_stride（= KW×I×O）等，LoadController 使用 vaddr + row_counter * stride       
  构造每行 DMA 请求                                                                                                                               
                                                                                                                                                  
  1.3 Tiling 决策算法                                                                                                                             
                  
  tiled_conv() → sp_tiled_conv() 的 tiling 参数由软件静态计算，核心约束是不超过 SPAD 和 Accumulator 容量：                                        
                  
  SPAD 总容量   = 256 KB = 256 × 1024 bytes                                                                                                       
  Bank 数量     = 4，每 bank 含 BANK_ROWS = (256KB / 4bank) / DIM bytes/row = 4096 rows（DIM=16时）                                               
  Accumulator   = 64 KB，2 banks，每 bank 4096 rows（int32 × DIM = 64 bytes/row）                                                                 
                                                                                                                                                  
  Tiling 决策遵循以下优先级：                                                                                                                     
  1. kch（kernel channels per tile） 必须整除 DIM=16（硬件 mesh 宽度），通常 kch = min(IC, DIM)                                                   
  2. orows × ocols × och 决定 Accumulator 占用：每个输出 tile 需要 orows × ocols × och / DIM 个 Accumulator 行                                    
  3. 输入 tile 占用 SPAD 的 A 区域，权重 tile 占用 B 区域，两者之和不超过 SPAD 总行数                         
  4. concurrent_loops=2 ping-pong 时，每个子区域再对半分                                                                                          
                                                                                                                                                  
  ---                                                                                                                                             
  二、SPAD 地址空间分配、数据 Layout 与 Tile 生命周期                                                                                             
                                                                                                                                                  
  2.1 LocalAddr 32 位编码
                                                                                                                                                  
  Gemmini 使用统一 32 位 LocalAddr 寻址 SPAD 和 Accumulator：                                                                                     
  
  bit 31       : is_acc_addr  (1 = Accumulator, 0 = SPAD)                                                                                         
  bit 30       : accumulate   (仅 acc 有效：1=累加到现有值, 0=覆写)                                                                               
  bits [29:0]  : 物理行地址                                                                                                                       
                                                                                                                                                  
  物理行地址到 Bank/Row 的映射（SPAD，sp_banks=4=2²）：                                                                                           
                                                                                                                                                  
  sp_bank() = addr[1:0]           // 低 2 位 → bank 编号 0..3                                                                                     
  sp_row()  = addr >> 2           // 高位 → 该 bank 内行号                                                                                        
                                                                                                                                                  
  即地址以**行间交织（interleaved）**方式分布到 4 个 bank：地址 0→bank0，1→bank1，2→bank2，3→bank3，4→bank0，…                                    
                                                                                                                                                  
  2.2 SPAD 地址空间区域划分（软件侧约定）                                                                                                         
                  
  gemmini.h 中的 sp_tiled_matmul_os/ws 建立了如下约定（DIM=16，BANK_NUM=4，BANK_ROWS=SPAD总行数/4）：                                             
                  
  A 区域起始 = 0                                                                                                                                  
      → 从地址 0 向上增长，存储当前 tile 的输入激活                                                                                               
      → 占用 I_tile × K_tile / DIM 行（每行 = DIM 个 int8）                                                                                       
                                                                                                                                                  
  B 区域起始 = BANK_NUM × BANK_ROWS - K × J × DIM                                                                                                 
      → 从 SPAD 顶部向下分配，存储权重                                                                                                            
      → 使 B 区域紧贴 SPAD 顶部，与 A 区域之间留最大缓冲                                                                                          
                                                                                                                                                  
  D/C 区域起始 = 1 << (ADDR_LEN - 1)   即 bit 31 = 1                                                                                              
      → 进入 Accumulator 地址空间                                                                                                                 
      → D（bias）: D_sp_addr_start = 1 << (ADDR_LEN-1)                                                                                            
      → C（output）: C_sp_addr_start = 3 << (ADDR_LEN-2)（Accumulator 上半区）                                                                    
                                                                                                                                                  
  关键设计意图：A 从底部向上、B 从顶部向下，两者碰撞即触发 tiling 约束违例。软件在静态 tiling 时已确保两者不重叠。                                
                                                                                                                                                  
  2.3 数据对齐要求                                                                                                                                
                  
  所有 tile 尺寸必须是 DIM=16 的倍数（mesh 物理宽度）：                                                                                           
  - 不足 16 的维度（如 kernel_channels < 16）由软件 zero-padding 到 16
  - gemmini_nn.h 中的 pad_C/pad_K 字段记录 padding 量                                                                                             
  - LoadController 在 DMA 读取时通过 pixel_repeats / block_stride 机制处理非对齐情况
                                                                                                                                                  
  2.4 Tile 生命周期                                                                                                                               
                                                                                                                                                  
  [DRAM] ─DMA→ [SPAD A/B 区] ─PRELOAD→ [PE array 权重寄存器]                                                                                      
                                ─COMPUTE→ [PE array MAC 运算]                                                                                     
                                ─→ [Accumulator] ─STORE→ [DRAM]
                                                 ─累加→ (多 K-tile 迭代)                                                                          
                                                                                                                                                  
  具体状态转移：                                                                                                                                  
  1. LOAD 阶段：LoadController 发出 DMA 请求，分 row_counter 行逐行搬运，写入 SPAD 指定 bank/row。DMACommandTracker 追踪 in-flight                
  请求，完成后通知 ReservationStation 清除 deps_ld 依赖位。                                                                                       
  2. PRELOAD 阶段：ExecController 发出 PRELOAD 指令，将 B tile 从 SPAD 预加载进 PE 的 c1/c2 权重寄存器。对 K 维度的第二块及以后，PRELOAD 使用
  GARBAGE_ADDR 跳过，实现权重 reuse（WS dataflow 核心优化）。                                                                                     
  3. COMPUTE 阶段：ExecController 连续发出 COMPUTE_PRELOADED / COMPUTE_ACCUMULATE 指令，A tile 行被逐行泵入 mesh，partial sums 垂直流过 PE        
  阵列，最终落入 Accumulator。                                                                                                            
  4. STORE 阶段：所有 K-tile 迭代完成后，StoreController 将 Accumulator 中的 int32 结果量化（accScale）后写回 DRAM。对于多输出 batch，Accumulator 
  双缓冲允许 STORE 与下一轮 COMPUTE 并行。                                                                                                       
  5. 回收：SPAD 中的 A/B tile 在 STORE 完成后即"释放"——Gemmini 无 GC，下一个 tile 的 LOAD 直接覆写相同地址区域。                                  
                  
  ---                                                                                                                                             
  三、Bank 排布、Striping 算法与冲突规避
                                                                                                                                                  
  3.1 Bank Striping 算法
                                                                                                                                                  
  SPAD 4 个 bank 以地址低 2 位做交织：

  行地址 a → bank = a % 4 = a[1:0]
           → bank内行 = a / 4 = a >> 2                                                                                                            
                                                                                                                                                  
  对于一个 16×16 tile（A 矩阵，16 行），地址 0..15 分布为：                                                                                       
  地址  0 → bank0 row0                                                                                                                            
  地址  1 → bank1 row0                                                                                                                            
  地址  2 → bank2 row0                                                                                                                            
  地址  3 → bank3 row0
  地址  4 → bank0 row1                                                                                                                            
  地址  5 → bank1 row1                                                                                                                            
  ...                 
  地址 15 → bank3 row3                                                                                                                            
                      
  一次 mesh 读取（16 列同时激活）恰好跨越 4 个 bank 的连续 4 行，每 bank 贡献 4 行 × 16 列 = 64 bytes，理论带宽 = 4 bank × 64 bytes/cycle。
                                                                                                                                                  
  3.2 Ping-Pong Buffer 实现（concurrent_loops=2）                                                                                                 
                                                                                                                                                  
  LoopMatmul 和 LoopConv 内部维护两个并发 loop 槽，head_loop_id 和 tail_loop_id = ~head_loop_id（1 bit 取反，值 0↔1 交替）。                      
                  
  SPAD 地址空间沿最高有效位（ADDR_LEN-1 位，bit 在 LocalAddr 非 acc 范围内）平分：                                                                
  - Loop 0（head）：使用地址 [0, max_spad_rows/2) 的 A/B 区
  - Loop 1（tail）：使用地址 [max_spad_rows/2, max_spad_rows) 的 A/B 区                                                                           
                                                                       
  即 ping-pong 通过将 loop_id 映射到 SPAD 地址的最高位实现：                                                                                      
                                                                                                                                                  
  sp_addr_start = loop_id * (sp_capacity / 2 / DIM)                                                                                               
                                                                                                                                                  
  执行流：                                                                                                                                        
  1. Loop 0 (head) 处于 LOAD 阶段，Loop 1 (tail) 处于 COMPUTE/STORE 阶段，两者访问不同 SPAD 区域，无 bank 冲突。                                  
  2. all_completed(tail_loop_id) 为真时，head_loop_id := ~head_loop_id，角色互换。                                                                
  3. ReservationStation 的 deps_ld/deps_ex/deps_st 位向量确保在 STORE 完成前不发出新 COMPUTE（防止 Accumulator 中间结果被覆盖）。
                                                                                                                                                  
  Accumulator 同理：两个 loop 使用 Accumulator 的上半区（C_sp_addr_start = 3<<(ADDR_LEN-2)）和下半区（D_sp_addr_start = 1<<(ADDR_LEN-1)）交替。   
                                                                                                                                                  
  3.3 Im2Col Bank 冲突检测与规避                                                                                                                  
                                                                                                                                                  
  Im2Col FSM 在 doing_im2col 状态每周期发出最多 3 个并发 SPAD 读请求（针对 3×3 等宽 kernel）：                                                    
                  
  主请求 bank    = (copy_addr + window_address) % 4                                                                                               
  相邻右 bank    = (copy_addr + window_address + 1) % 4                                                                                           
  相邻左 bank    = (copy_addr + window_address - 1) % 4   // 仅宽kernel需要
                                                                                                                                                  
  冲突规避机制：  
                                                                                                                                                  
  1. 权重 bank 排除：ExecController 维护 weight_double_bank 和 weight_triple_bank 标志。当权重预加载占用某些 bank 时，Im2Col                      
  读请求被推迟（stall），等待该 bank 空闲后再发出。
  2. 单端口 SPAD 仲裁：SPAD 每个 bank 是 single-ported。多路访问（DMA writeback vs Im2Col read vs direct SPAD read）由 Scratchpad                 
  内部的优先级仲裁器处理。Im2Col 请求具有较低优先级，DMA 写入优先。                                                                               
  3. im2col_en 门控：val im2col_en = config.hasIm2Col.B && weight_stride =/= 0.U。仅当 LOOP_CONV_WS 路径（weight_stride
  非零）时激活。在此条件下，ExecController 将直接 SPAD 读路径禁用（!(im2col_wire && im2col_en)），确保激活数据读取路径唯一性——要么直接读          
  SPAD，要么经 Im2Col 单元，不能同时激活。
                                                                                                                                                  
  ---             
  四、Window-Counter 到物理 SPAD 地址推导
                                                                                                                                                  
  4.1 两级 Window Counter 语义
                                                                                                                                                  
  Im2Col 单元维护两个计数器，对应卷积滑动窗口的两个维度：                                                                                         
  
  window_start_counter : 当前窗口在输入 SPAD 中的"起始行偏移"                                                                                     
                         随输出空间坐标 (orow, ocol) 推进                                                                                         
  window_row_counter   : 当前窗口内的"行内偏移"                                                                                                   
                         在 [0, kH×kW) 范围内遍历 kernel 行                                                                                       
                                                                                                                                                  
  两者相加得到 逻辑 SPAD 行地址：                                                                                                                 
                                                                                                                                                  
  window_address = window_start_counter + window_row_counter                                                                                      
                  
  4.2 物理地址推导公式                                                                                                                            
  
  设 copy_addr 为 LoadController 将 Input tile 写入 SPAD 时的起始 LocalAddr（由 LoopConv.LdInput 计算并传入 Im2Col）：                            
                  
  logical_addr = copy_addr.to_uint() + window_address                                                                                             
                                                                                                                                                  
  physical_bank = logical_addr % sp_banks
               = logical_addr[1:0]          // 低 2 位                                                                                            
                                                                                                                                                  
  physical_row  = logical_addr / sp_banks                                                                                                         
               = logical_addr >> 2          // 高位                                                                                               
                                                                                                                                                  
  对应 Im2Col.scala:146–148：                                                                                                                     
  
  val window_address = window_start_counter + window_row_counter                                                                                  
  val im2col_spad_addr = copy_addr + window_address                                                                                               
  val im2col_spad_bank = im2col_spad_addr.sp_bank()   // 即 [1:0]
  val im2col_spad_row  = im2col_spad_addr.sp_row()    // 即 >> 2                                                                                  
                  
  4.3 window_start_counter 推进逻辑                                                                                                               
                  
  window_start_counter 对应输出像素 (orow, ocol) 在输入空间中的左上角位置，以 SPAD 行为单位：                                                     
                  
  input_spad_row(orow, ocol) = orow × stride × input_w_in_spad_rows                                                                               
                             + ocol × stride × channels_per_spad_row                                                                              
                                                                                                                                                  
  其中 input_spad_stride（LoopConv.derived_params() 计算）= 每输入行占用的 SPAD 行数 = ceil(input_W × C / DIM)。                                  
                                                                                                                                                  
  每次 window_row_counter 遍历完一个 kernel row（达到 kW × kCH / DIM），window_start_counter 增加 input_spad_stride（即移动到输入的下一 spatial   
  行），继续用 window_row_counter 遍历该行的所有 kernel 列。
                                                                                                                                                  
  4.4 完整端到端地址计算示例

  以 ResNet50 第一层卷积为例（简化参数）：                                                                                                        
  Input: 224×224×3, padded to 224×224×16（channel padding到DIM）
  Kernel: 7×7，Output: 112×112×64，stride=2                                                                                                       
  Input tile: 14×14×16（一个 orow tile 对应 patch）                                                                                               
  copy_addr（Input tile起始）= 0（loop head SPAD 下半区）                                                                                         
                                                                                                                                                  
  对于输出像素 (orow=0, ocol=0)，window_start_counter = 0。                                                                                       
  访问 kernel 位置 (kr=0, kc=0)：window_row_counter = 0 × 1 + 0 = 0。                                                                             
  im2col_spad_addr = 0 + 0 = 0 → bank0, row0。                                                                                                    
                                                                                                                                                  
  访问 (kr=0, kc=1)：window_row_counter = 1。                                                                                                     
  im2col_spad_addr = 1 → bank1, row0。                                                                                                            
                  
  访问 (kr=1, kc=0)：window_start_counter += input_spad_stride（设=1，即224×16/DIM=224行压缩到224行）→ window_start_counter=224。
  im2col_spad_addr = 224 + 0 = 224 → bank0（224%4=0）, row56（224/4=56）。
                                                                                                                                                  
  输出像素 (orow=0, ocol=1)（stride=2，向右滑2列）：                                                                                              
  window_start_counter 增加 stride × channels_per_spad_row = 2 × 1 = 2（列方向偏移2个SPAD行）。                                                   
  im2col_spad_addr = 2 → bank2, row0。                                                                                                            
                                                                                                                                                  
  以上展示了 window_counter 如何将卷积的 2D 非线性访问模式折叠为 SPAD 的 1D 行地址序列，从而使 Im2Col 单元能够以 1 cycle/row 的速率将输入 patch   
  线性化并送入 mesh。                                                                                                                             
                                                                                                                                                  
  ---             
  总结：内存管理设计哲学
                                                                                                                                                  
  Gemmini 的内存管理体现了静态分析、硬件简化、软件承担复杂度的设计哲学：
                                                                                                                                                  
  ┌───────────────────┬─────────────────────────────┬───────────────────────────────┐                                                             
  │       层次        │           管理者            │             机制              │                                                             
  ├───────────────────┼─────────────────────────────┼───────────────────────────────┤                                                             
  │ DRAM 全局分配     │ 软件（C 数组）              │ 静态声明，指针传递            │
  ├───────────────────┼─────────────────────────────┼───────────────────────────────┤
  │ Tiling 参数计算   │ 软件（tiled_conv）          │ 静态约束求解                  │                                                             
  ├───────────────────┼─────────────────────────────┼───────────────────────────────┤                                                             
  │ SPAD 区域划分     │ 软件约定（A底/B顶/Acc独立） │ LocalAddr 编码                │                                                             
  ├───────────────────┼─────────────────────────────┼───────────────────────────────┤                                                             
  │ Ping-Pong 切换    │ 硬件（LoopMatmul/Conv）     │ head/tail ID + 地址位翻转     │
  ├───────────────────┼─────────────────────────────┼───────────────────────────────┤                                                             
  │ Bank Striping     │ 硬件（sp_bank()）           │ addr % 4 自动交织             │
  ├───────────────────┼─────────────────────────────┼───────────────────────────────┤                                                             
  │ Im2Col 非线性访问 │ 硬件（Im2Col FSM）          │ window_counter + 物理地址映射 │
  ├───────────────────┼─────────────────────────────┼───────────────────────────────┤                                                             
  │ 依赖追踪/冲突检测 │ 硬件（ReservationStation）  │ 地址范围重叠检测 + dep位向量  │
  └───────────────────┴─────────────────────────────┴───────────────────────────────┘                                                             
                  
  这种分层使得硬件 datapath 极度简化（单端口 SRAM + 简单仲裁），同时通过软件静态 tiling 和硬件 window-counter                                     
  的协同，实现了接近理论峰值的带宽利用率。
```