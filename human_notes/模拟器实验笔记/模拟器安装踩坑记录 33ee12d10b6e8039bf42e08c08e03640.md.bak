# 模拟器安装踩坑记录

模拟器构建，可学Cpp。

0、docker使用

容器只要不rm，其中的修改在关闭容器后不会丢失，修改被commit后创造新image。

volume绑定（-v）到host path（或name或noname）和image path是host和容器的共享区域，不会随着容器销毁而销毁。

非空volume的内容会覆盖容器中绑定区域，空白volume被容器内容同步（覆盖）。

volume是传递不同容器内容的缓冲区，volume中修改被commit时被忽略而不会更新到image，非volume的修改（环境依赖）会更新到image。

1、创造新的image：如code cli在docker中

—network host。

.devcontainer/devcontainer.json配置模板。

node需要设置curl安装较新版本（≥18）。

postcmd无法使用host vpn，因此执行失败，最好在容器内构建AI环境后重新提交成新版本image。

```bash
docker run -it --name temp_builder --network=host image_name:version_name /bin/bash

# 1. 更新源并安装 curl
apt-get update && apt-get install -y curl
# 2. 下载 Miniconda 安装包
curl -O https://repo.anaconda.com/miniconda/Mini conda3-latest-Linux-x86_64.sh
# 3. 静默安装
bash Miniconda3-latest-Linux-x86_64.sh -b -p /root/miniconda3

# 1. 临时激活 Conda 并初始化环境
source /root/miniconda3/bin/activate
conda init bash
source ~/.bashrc
# 2. 接受 Anaconda 服务条款 (2026版必须步骤)
conda tos accept --channel https://repo.anaconda.com/pkgs/main
# 3. 创建 Python 3.10 环境并安装工具
conda create -n ai_env python=3.10 -y
conda run -n ai_env pip install code-review-graph

# 1. 安装 Node.js 20 官方源
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt-get install -y nodejs
# 2. 全局安装 AI 工具
npm install -g @anthropic-ai/claude-code @openai/codex

# 1. 退出容器
exit
# 2. 将此容器保存为 v3 镜像
docker commit temp_builder image_name:version_name
# 3. 删除临时构建容器
docker rm temp_builder
```

2、chipyard容器

source env.sh.

bugfix: `override SBT_OPTS += -DfirrtlVersion=1.4.1` 。[https://zhuanlan.zhihu.com/p/1891956556226085660](https://zhuanlan.zhihu.com/p/1891956556226085660)

glibc2.31升级vscode remote：deb url，pubkey，apt update。

stb缺失本地库依赖，远程snap删除无法获取，导致make失败：需要stb set version publishlocal。

![Screenshot from 2026-04-12 17-17-12.png](%E6%A8%A1%E6%8B%9F%E5%99%A8%E5%AE%89%E8%A3%85%E8%B8%A9%E5%9D%91%E8%AE%B0%E5%BD%95/Screenshot_from_2026-04-12_17-17-12.png)

sbt 'set version := "1.5-SNAPSHOT"' publishLocal. treadle1.5-snap/firrtl1.3-snap.

官方docker的toolchain path设置不对：软链接ln /root/*到/root/chipyard/*。

3、gemmini conda

conda不支持glibc2.35，conda-req根据jammy选择glibc2.35：修改build-setup，强制设置成conda支持版本（2.34）。

4、torchsim容器（to设置CC）

较新，顺利。

5、onnxsim容器（to设置CC）

较新，顺利。