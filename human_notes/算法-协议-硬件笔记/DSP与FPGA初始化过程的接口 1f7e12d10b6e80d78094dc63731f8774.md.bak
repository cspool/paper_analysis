# DSP与FPGA初始化过程的接口

![image.png](DSP%E4%B8%8EFPGA%E5%88%9D%E5%A7%8B%E5%8C%96%E8%BF%87%E7%A8%8B%E7%9A%84%E6%8E%A5%E5%8F%A3/image.png)

**FLASH数据初始化需求**

1. 将权重、图像通过CCS保存至DSP端DDR存储空间，将DSP中DDR数据写入至DSP的flash中，FLASH写操作API驱动需配置信息如下，地址需512bit对齐，长度信息为字节长度：

1）DSP端DDR首地址与数据Length长度信息

2）FLASH端地址

2、**上电启动**后将上述数据加载到DSP的DDR上，FLASH读操作API驱动需配置信息如下，地址需512bit对齐，长度信息为字节长度：**（正式推理时，直接从FLASH将数据整体传输至FPGA-DDR，而不用通过DSP控制初始化）**

1）FLASH端地址与数据Length长度信息

2）DSP端DDR首地址

**EMIF接口需求**

1. EMIF与指令**存储器**接口，备注采用**真双端口**BRAM（存储空间大小256*512b）
    1. 用户端512bit；
    2. 外协端8/16/32(自行定义即可)
    3. 首地址采用参数化处理即可ADDR_RAM_IMG
2. EMIF与权重参数（Bias,Scale,E）**存储器**接口，备注采用**真双端口**BRAM（存储空间大小2048*512b）
    1. 用户端512bit；
    2. 外协端8/16/32(自行定义即可)
    3. 首地址采用参数化处理即可ADDR_RAM_WB
3. EMIF与命令和状态**寄存器**接口，备注每组个10个
    1. 用户端32bit；
    2. 寄存器名称处理即可REG_CMD0~9，REG_STATUS0~9
    3. 采用REG_CMD中任意一个bit控制谁使用DDR控制器；

**SRIO接口需求：（DSP控制FPGA-DDR数据的初始化，便于调试，FLASH向DDR的写入只在上电时进行，FLASH进行的初始化适合online，而不是debug）**

1、DSP通过SRIO端口将DSP端DDR数据写入FPGA端DDR上，SRIO写操作API驱动需配置信息如下，地址需512bit对齐，长度信息为字节长度：

1）DSP端DDR首地址与Length长度信息

2）FPGA端DDR首地址

2、DSP通过SRIO端口将FPGA端DDR数据回读至DSP端DDR上，SRIO读操作API驱动需配置信息如下，地址需512bit对齐，长度信息为字节长度：

1）FPGA端DDR首地址与Length长度信息

2）DSP端DDR首地址

3、开展读写强度测试工作，

1）重复步骤1、2，反复1000次，保证SRIO通讯无误码

**DDR设计需求：**

1）DDR避免割裂，采用统一控制器设计，数据位宽采用北航接口设计（Addr-29bit，Data-512bit），**采用REG_CMD中任意一个bit控制谁使用DDR控制器；**