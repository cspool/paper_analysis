# 超参数进化的核心流程

1. **初始化种群**：随机生成若干组超参数组合（默认种群大小为 `300`）。
2. **评估适应度**：每组超参数训练模型，以验证集 mAP 作为适应度指标。
3. **选择与交叉**：保留适应度高的个体，进行交叉重组生成子代。
4. **变异**：对子代施加随机扰动（如增减学习率），增加多样性。
5. **迭代优化**：重复步骤 2~4，直到达到指定进化代数或收敛。