# SyncBN

### **1. SyncBN 的核心作用**

- **问题背景**：普通 BN 在单卡上计算当前批次的均值和方差，但在多卡训练时，每卡仅基于本地小批次计算统计量，导致全局统计量偏差。
- **SyncBN 解决方案**：跨所有 GPU 同步计算全局均值和方差，确保归一化统计量的一致性。
- **效果**：
    - 提升多卡训练时的模型收敛速度和精度（尤其是小批次场景）。
    - 减少因统计量偏差导致的训练不稳定。