# rect

| **特性** | **矩形训练（`--rect`）** | **传统方形训练** |
| --- | --- | --- |
| **输入尺寸** | 动态调整（如 `640x512`） | 固定正方形（如 `640x640`） |
| **填充区域** | 最小填充，减少无效像素 | 可能大量填充 |
| **显存占用** | 更低，允许更大批次或更高分辨率 | 更高 |
| **训练速度** | 更快（减少冗余计算） | 较慢 |
| **模型泛化性** | 更好（适应多尺度目标） | 可能略差 |