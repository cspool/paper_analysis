## Unlocking Pipeline Parallelism for Bootstrapping: A Pipelined Multi-Chiplet TFHE Accelerator

- 属于芯片设计的实现是什么？实验比较什么？
  - 实现是 CASCADE 的多芯粒（multi-chiplet）物理架构：12 个 HMUX Chiplet（HC）按 4×3 网格、环形拓扑互连，通过高速 die-to-die（D2D）链路（UCIe Advanced 规范，16 GT/s、64-bit 数据宽 → 1024 Gbps）连接；环形拓扑让 HC_C-1 回传数据给 HC_0，支持深流水（n 可远大于 C，RLWE 在环上多次循环）。关键芯片设计创新是 BSK-distributed 分布式内存层次：把集中式大容量内存转换为分布在每个 chiplet 的小型 SRAM（每 HC 10.5 MB BSK SRAM + 1 MB 缓冲，共 126 MB），足以驻留全部 BSK（参数集 III 需 112 MB、IV 需 90 MB），消除 BSK 片外搬运并把高并发 BSK 访问限制在各 chiplet 内。封装采用成熟的 2.5D 无源硅中介层（低复杂度；整体规模/互连密度/封装复杂度在 Intel Sapphire Rapids 等现有工业多芯粒产品已验证范围内）。设计空间探索（DSE）：单 HC die 面积约束 50-150 mm²（工业成熟高良率区）、芯粒数 C∈[4,32]，固定总面积预算下最大化 Throughput/Area，最优 C=12（C<12 时加芯粒增并行收益；C>12 时每芯粒承担 D2D PHY/互连逻辑固定面积税，计算面积被压缩，性能/面积下降）。单 HC 92.5 mm²/29.91 W（TSMC 28nm、1.2 GHz 综合），HC0 加 VPU 额外 60.1 mm²，全系统 1170.1 mm²/372.72 W。
  - 实验比较：(1) 与集中式内存架构对比——MP-PP（Morphling 缩放到与 CASCADE 相同面积 + 使能跨核通信支持流水线并行）：集中式 HBM 内存无法支撑跨 HMUX 流水线的并发 BSK 访问，带宽饱和，MP-PP 利用率仅 14.3%，Speedup/Area 反而低于原 Morphling；MP-PP-HBM（加 HBM stack 到足以供应流水线带宽，需 8 个 stack）：CASCADE 的 Speedup/Power 高 3.7×（单 HBM stack 约 30W，8 个 stack 功耗负担巨大，HBM 功耗不可持续）。(2) 可扩展性——CASCADE-x（不同 HC 数）在 DeepCNN-100 上端到端时延随芯粒数增加持续下降：BSK-distributed 保证片外带宽压力不随芯粒数增长，BSK-stationary 数据流使 ICT 只发生在物理相邻 chiplet 间避免通信拥塞。(3) SRAM 容量敏感性——总 BSK SRAM 从 28 MB 扫到 160 MB（保持芯粒数不变）：容量不足被迫片外取数性能骤降；超过临界阈值（126 MB 配置足以容纳 128-bit 参数）性能达峰，继续加容量边际收益可忽略。
- 模拟器名，模拟器链接（web search），或论文修改的模拟器。
  - 论文自研 cycle-accurate 模拟器（基于 F1 [32] 方法，论文未给链接，联网搜索 2026-08 无法确认公开版本），建模 HC 微架构 + D2D 通信。
  - D2D 物理参数依据 UCIe 规范 [25]（Das Sharma et al., Universal Chiplet Interconnect Express，开放行业标准）与 UCIe Advanced [27]（arXiv:2510.06513，提供 D2D 带宽/PHY 面积功耗模型）；论文未给出链接。芯片面积/功耗由 Synopsys Design Compiler + TSMC 28nm 库综合得到。
- 模拟器模拟什么的性能，修改了什么。
  - 模拟器模拟 CASCADE 多芯粒系统的执行时间：按 cycle 推进每个 HC 内功能单元流水线与 D2D 传输（16 GT/s、1024 Gbps），跟踪 BSP 数据依赖与 chiplet 间 ICT 通信，输出总执行时间、HC 资源利用率与 D2D 带宽利用率；集成 OIFS 调度器求最优 f(t,c) 映射并指导 BSK 放置。修改点：建模环形拓扑 D2D 链路时序与 PHY 面积税、分布式 SRAM 容量/带宽（BSK-distributed）、BSK-stationary 数据流、interleaved-fusion 组内回馈路径、HC 数/内部并行度可配置（DSE 扫描 C 与 IP）、SRAM 容量扫描，以及 MP-PP / MP-PP-HBM（Morphling + 跨核通信、额外 HBM stack）等替代方案建模。
- 开源情况。基于开源文档和论文，使用例子解释模拟器如何使用？作用是什么？
  - 开源情况：CASCADE 芯片/模拟器未开源（论文未给链接，联网搜索 2026-08 未找到仓库，无法确认）；UCIe 为开放行业标准（16 GT/s 每 lane 的 D2D 接口规范），PHY 面积/功耗参考 [25][27]；封装沿用 2.5D 无源硅中介层等成熟工业方案（无专用模拟器）。
  - 使用例子（芯片级数据路径）：输入 = 12 HC 配置（4×3 网格、环形拓扑、每 HC 10.5 MB BSK SRAM + 1 MB 缓冲、UCIe 16 GT/s 64-bit D2D、TSMC 28nm @1.2 GHz、IP=256）+ 应用（如 DeepCNN-50，参数集 II，BSP 数 B）+ BSK（126 MB 总量）。① OIFS 把 n 个 HMUX 划分/交错成 f(t,c) 映射，指导 BSK 在各 chiplet 的放置；② 每个 HC 从本地 SRAM 取 BSK_i 执行 HMUX（Rotation→Decomp→FFT→VMA→IFFT 系数粒度流水），ACC 经 128 KB 输出 double buffer 由 D2D 传给环形下一 HC（组内融合时回馈本地输入），HC 同时接收上游 RLWE——计算与 D2D 传输重叠；③ 当 n>C 时 RLWE 在环上多次循环直至 n 次迭代完成，之后 HC0 的 VPU 做 key-switching 输出 LWE 密文。作用：在流片前验证"分布式 SRAM 替代 HBM 能否支撑跨 HMUX 流水线带宽"这一芯片级决策，量化 chiplet 数/PHY 面积税与并行的折中，指导 C=12、IP=256、126 MB SRAM 的最终物理配置，并证明多芯粒方案相对加 HBM stack 方案在功耗/面积上的可持续性（3.7× 每瓦优势）。
