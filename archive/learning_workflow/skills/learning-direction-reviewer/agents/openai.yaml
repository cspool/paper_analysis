# Archived legacy agent profile; no longer discoverable from .codex/skills.
interface:
  display_name: "Learning Direction Reviewer"
  short_description: "Deep-review one Direction through an evidence loop"
  default_prompt: "Use $learning-direction-reviewer to advance one expert review action for the supplied Direction."
