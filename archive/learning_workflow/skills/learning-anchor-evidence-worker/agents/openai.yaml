# Archived legacy agent profile; no longer discoverable from .codex/skills.
interface:
  display_name: "Learning Anchor Evidence"
  short_description: "Extract atomic Anchor evidence from local notes"
  default_prompt: "Use $learning-anchor-evidence-worker to answer one discovery task with verified atomic claims."
