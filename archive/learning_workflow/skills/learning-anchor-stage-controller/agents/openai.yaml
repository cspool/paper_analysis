# Archived legacy agent profile; no longer discoverable from .codex/skills.
interface:
  display_name: "Learning Anchor Controller"
  short_description: "Plan diverse Anchor exploration rounds and convergence"
  default_prompt: "Use $learning-anchor-stage-controller to plan the next Anchor exploration round from canonical state."
