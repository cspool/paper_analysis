# Archived legacy agent profile; no longer discoverable from .codex/skills.
interface:
  display_name: "Learning Direction Planner"
  short_description: "Build compatible experimental Direction subgraphs"
  default_prompt: "Use $learning-direction-planner to propose one distinct compatible Direction for a single Anchor."
