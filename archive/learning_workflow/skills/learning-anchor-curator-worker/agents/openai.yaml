# Archived legacy agent profile; no longer discoverable from .codex/skills.
interface:
  display_name: "Learning Anchor Curator"
  short_description: "Curate verified claims into Anchor graph deltas"
  default_prompt: "Use $learning-anchor-curator-worker to convert verified claims into atomic Anchor, baseline, entry, and edge deltas."
