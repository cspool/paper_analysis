interface:
  display_name: "Semantic Loop Evidence Reader"
  short_description: "Search and deep-read one bounded Obsidian need"
  default_prompt: "Use $learning-semantic-loop-evidence-reader to answer one bounded SearchNeed with a traceable EVIDENCE_PACKET."
dependencies:
  tools:
    - type: "mcp"
      value: "obsidian"
      description: "Read-only local Obsidian search and note retrieval"
policy:
  allow_implicit_invocation: true
