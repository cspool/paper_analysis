## Unified flow-control & scheduling engine（统一流控调度引擎）

术语是什么？回答尽量完整，回答逻辑链中每一步都解释出来。通过联网搜索让回答具体和精准。
事务层中替代"每协议独立队列"的统一调度器：单一引擎协调 CXL.io/CXL.cache/CXL.mem 三协议的报文排序、分组与派发，按流量模式自适应调节派发行为，并保证各消息类间公平。PCIe 派生设计每协议各有一套队列，混合流量下产生队列争用、优先级不均与长处理延迟。硅片实测较按协议分队列 +1.3× 吞吐。

从硬件架构角度拆解术语，比如术语如何在硬件架构中发挥作用，给出术语在硬件架构中运转流程的具体例子。通过联网搜索让回答具体和精准。
统一调度流程：三协议请求进入共享流控单元 → 调度器按（协议优先级、流量模式、下游流水线占用、链路利用率）选择下一报文 → 注入统一流水线 → 链路层 flit 打包。配合跨层反馈（物理层跟踪链路活动调节数据释放、事务层按链路利用率选报文），流水线在流量波动下自调节，最小化 idle cycle 并保持下游流水线利用率稳定。

术语一般如何实现？如何使用？通过联网搜索让回答具体和精准。
作为控制器事务层 RTL 模块：输入为三协议请求队列，输出到链路层 flit 打包。适用于混合流量（io/cache/mem 并发、读写混合、多主机一致性消息）场景，避免某协议饿死与 head-of-line blocking。本论文中它是统一控制器"连续数据打包（continuous data packing）"的核心（论文 Fig. 7）。

涉及论文标题：
- A Silicon-Proven Unified Low-Latency CXL Controller and Port-Based Routing Switch for Memory-Centric Fabrics
