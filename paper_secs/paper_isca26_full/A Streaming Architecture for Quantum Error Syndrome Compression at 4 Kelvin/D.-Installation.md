# *D. Installation*

```
$ unzip icepack-artifact.zip
$ cd artifact
$ docker build -t icepack-artifact .
```

# *D. Installation*

```
$ unzip icepack-artifact.zip
$ cd artifact
$ docker build -t icepack-artifact .
```

