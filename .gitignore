.obsidian/
.claudian/
outputs/
__pycache__/

# Regenerable per-paper model event streams and batch console logs. Keep the
# compact progress.json and status/ records for resumability and audit.
/paper_extract_checkpoints/**/logs/
/paper_extract_checkpoints/**/*.log

.claude/learning-runs/
.claude/idea-review-runs/

# Local editor metadata; this project has no shared VS Code workspace contract.
/.vscode/

# Regenerable Learning Workflow runs. Curated audit snapshots belong in
# archive/learning_workflow/official_runs/ instead.
/learning_outputs_codex/

# Regenerable Direction Experiment runs (provider logs, environments, model/data
# caches, raw measurements, and per-run workspaces). Curated reports, configs,
# and minimal patches that merit versioning belong under archive/ instead.
/experiment_outputs_codex/

# Regenerable experiment model, checkpoint, data, and environment caches.
/experiment_cache/

# One-off, machine-specific document conversion helper.
/scripts/md2pdf_reference.sh
