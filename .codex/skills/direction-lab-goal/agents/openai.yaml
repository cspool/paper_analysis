interface:
  display_name: "Direction Lab Goal"
  short_description: "Execute one atomic Direction experiment contract"
  default_prompt: "Use $direction-lab-goal to execute one frozen atomic contract, evaluate stop conditions, and atomically preserve checkpoint or reviewable evidence."
