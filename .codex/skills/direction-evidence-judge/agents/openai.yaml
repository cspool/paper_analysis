interface:
  display_name: "Direction Evidence Judge"
  short_description: "Audit atomic experiment evidence and early stops"
  default_prompt: "Use $direction-evidence-judge to audit one atomic contract, its claimed early-stop boundary, and the actual evidence scope without scheduling the workflow."
