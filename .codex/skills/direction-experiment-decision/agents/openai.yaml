interface:
  display_name: "Direction Experiment Decision"
  short_description: "Freeze atomic contracts and choose the next branch"
  default_prompt: "Use $direction-experiment-decision to review the trajectory and freeze one feasible atomic experiment contract or choose the next workflow branch."
